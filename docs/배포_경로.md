# 배포 경로

> 이 저장소에서 「자연어 → 원격 배포」가 실제로 성립하는지 확인하는 길.
> **기능을 쌓기 전에 이 경로부터 뚫는다** — 3개월 뒤에 막히면 되돌리기가 가장 비싸다.

## 지금까지 뚫린 곳 (2026-08-03)

| 구간 | 상태 |
|---|---|
| 코드 → 번들 | ✅ `npx expo export` 성공 (웹 375KB) |
| 번들 → 화면 | ✅ 렌더 확인 · 콘솔 오류 0 |
| push → CI 검사 | ✅ `.github/workflows/ci.yml` (테스트 29 + 교정 평가 26) |
| CI → EAS 빌드 | ⛔ **EXPO_TOKEN 없음** — 아래 절차 |
| 빌드 → 스토어 | ⛔ Apple $99/년 · Google $25 (나중) |
| 코드 → OTA 도착 | ⛔ EAS 프로젝트 연결 뒤 |

앱을 열면 **배포 도착 확인 화면**이 나온다(`App.js`). 여기 「업데이트 ID」가 바뀌면 새 코드가 도착한 것이다. **「배포했다」는 밀었다는 뜻이지 도착했다는 뜻이 아니다.**

---

## 유호님이 하실 것 — 10분, 무료

### 1) Expo 계정 만들기

1. https://expo.dev 접속 → 오른쪽 위 **Sign Up**
2. 이메일·비밀번호로 가입 (GitHub 계정으로도 됩니다)
3. 가입 후 조직 이름을 물으면 개인 계정 그대로 두셔도 됩니다

> 💰 무료입니다. EAS 무료 등급 = 빌드 월 15회 + OTA 업데이트 월 1,000 MAU.

### 2) 액세스 토큰 발급

1. https://expo.dev/settings/access-tokens 접속
2. **Create token** 클릭
3. 이름에 `github-ci` 입력 → 만들기
4. 화면에 나온 긴 문자열을 **복사** — 이 창을 닫으면 다시 볼 수 없습니다

### 3) GitHub에 토큰 등록

1. https://github.com/unmet23-lab/SYNK-talk/settings/secrets/actions 접속
2. **New repository secret** 클릭
3. Name 칸: `EXPO_TOKEN`
4. Secret 칸: 2번에서 복사한 문자열 붙여넣기
5. **Add secret**

### 4) 알려주시면 나머지는 제가 합니다

- `eas init`으로 프로젝트 연결(app.json에 projectId 기록)
- 첫 preview 빌드 실행 → **APK 링크**를 드립니다. 폰에 설치해서 화면을 확인하실 수 있습니다.
- OTA 채널 배선

> ⚠ **토큰을 저에게 직접 보내지 마세요.** GitHub Secret에 넣으시면 제가 값을 몰라도 CI가 씁니다. 채팅·파일에 붙여넣지 마세요.

---

## 스토어 제출 (나중 — 지금 결제하지 마세요)

실제로 앱을 스토어에 올릴 때만 필요합니다.

- **Apple Developer** $99/년 — iOS. 개인 명의 가입 후 심사 2~7일.
- **Google Play Console** $25 1회 — Android. 신규 개인 개발자는 **폐쇄 테스트 12명 × 14일** 요건이 있어 시간이 걸립니다.

👉 **Android(Google)부터 여는 것을 권합니다** — 요금이 1회고, preview APK를 학생 폰에 바로 설치해 테스트할 수 있어 스토어 없이도 실사용 검증이 가능합니다.

---

## CI가 지금도 하는 일

`EXPO_TOKEN`이 없어도 push할 때마다 도는 검사:

- `npm test` — 커밋 가드 회귀 29건
- `npm run eval` — 교정 엔진 26문항 (거짓양성 0 아니면 빨간불)

빌드 job은 토큰이 없으면 **조용히 건너뜁니다.** 없는 자격증명 때문에 빨간불이 상시로 켜져 있으면 사람이 빨간불을 무시하게 되기 때문입니다.
