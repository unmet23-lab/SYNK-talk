# 강사 반 단위 피드백 + G2 「맞음」 자동 하강 설계 v2 — «한 벌»

> 유호 지시 2026-08-12: 「피드백도 선생님입장에서 하기 쉽게 만들어줘, 우리가 예전에 글라이드 앱으로
> 만들었던 기능처럼 반을 보기 쉽게 정해서 해당 반을 들어가서 피드백하기 쉽도록. 아니면 피드백 전용
> 기능이있는 메뉴를 만들어주던지 해줬으면 해.」 + 「뭐든 단단한 설계 올바른 설계가 우선이야. 니가 판단해줘」
>
> **정본 = 이 문서.** 인계 = 보드 `955e8a16` 🎫(「㉡ 한 벌 + 선생님 피드백 UX(반 단위)」).
> ⚠ **F381 — 같은 🎫 를 두 세션이 10분 안에 나란히 설계했다**(`1a1b47ad` `c7ed0d14` ↔ `5c3f51e1` `e8b0631`).
> 그 뒤 **서로 상대에게 넘기는 두 번째 사고**가 났다(내가 저쪽에 넘기고, 저쪽이 근거를 대고 나에게
> 넘겼다) — UX 절반이 잠시 주인 없이 떴다. **이 v2 가 그걸 닫는다: 🎫 전부를 여기서 진다.**
> 저쪽 문서는 폐기 스텁으로 남아 있고(`as/docs/강사_피드백_UX_설계.md` · 🚫 지우지 않는다),
> **거기서만 나온 실측 4줄은 아래 §2 에 회수했다.**
>
> 관련 정본 — `M2_라벨회로_설계.md`(역할 경계·골든 회로) · `검수_내부계약.md`(검수자 통로) ·
> `발주_게임모듈.md` §4(G2). 셋 다 **이 설계가 안 건드린다.**

## §0 한 장 요약 — 네 문장

① 「반」은 시트에만 물리로 있고 **원본(Postgres)엔 칸조차 없다** — 그런데 재료는 이미 오고 있고
인제스트가 그 열을 **버린다**. ② 강사 문은 이미 있다(`teach`) — 새 함수 0, 라우트 셋. ③ 유호님이
말한 「보기 쉽게」의 정답은 코드에 있다 — **목록이 아니라 카드**다. ④ G2 「맞음」 자동 하강은 큐
«조회 시점»이 아니라 **AI 교정 앞단**에 서야 한다 — 늦게 내리면 억지 교정 행이 골든 표본 풀에 남아
과교정률 계기판을 거짓으로 부풀린다.

## §1 선행 실측 — `955e8a16` 이 남긴 세 물음의 답

**ⓐ 「반」은 원본에 0이다. 그런데 재료는 이미 온다.**

| 잰 곳 | 결과 |
|---|---|
| `engine.learners` 열 전량(c6 기준선 + c7~c11 `alter` 전량) | 반/class 칸 **없음**(`level_current`·`home_aimag`·`goal_track` 은 있는데 반은 없다) |
| `engine.staff` | `role`·`active`·`revoked_before` 뿐 — **강사↔반 배정이 없다** |
| 마이그 전량 `class_id`·`class_name` / 표 20개 전량 | **0건** — `classes` 표 자체가 없다 |
| `roster-ingest` 가 건네는 것 | `student_code`·`contact`·`display_name`·`schema_ver` **넷뿐**(`index.ts:131`) |
| 명부 규칙 `lib/명부규칙.js` 의 `열별칭` | **번호·전화·이름·역할 4개뿐** |
| 시트 — 반 정본 | ✅ `schedule` 시트 `['반','요일유형','시작시간','정원','표시명']`(`as/Code.js:1198`) |
| 시트 — 학생→반 | ✅ 운영 `profiles` **5열이 `class_name`**(`as/Code.js:955-957`) · 상담시트 `반` 열 |
| 시트 — 강사→반 | ✅ `teacher_stats` 9열 `담당반`(**조인 결과** · `as/엔진_운영배치.js:2842`) |

- 🔴 **처방은 「반을 새로 만들자」가 아니라 «이미 오는 것을 받자»다.** 유호님이 내려받아 먹이는 그
  파일에 반이 이미 적혀 있는데 인제스트가 읽지도 않고 버린다. 사람이 반을 **다시 입력하는 칸은
  만들지 않는다**(철학 ㉡ 정면 — 사람 손을 늘리는 해법은 미룬 결함이다).
- 🔑 **키가 이미 있다** — `평일11A` 6자 고정. 유호 08-01 확정 「이름 3층 분리」(`as/Code.js:1184`):
  ①키(A열 · **영원히 안 바뀐다**) ②문패(코드 밖) ③표시명(E열 · 학생·학부모가 보는 이름 · 시즌 1
  이후 각 반이 직접 짓는다). **바뀌는 것은 ③뿐이다.**
- ⚠ **한 반 여러 강사(공동 담당)가 정상**이다 — 실측(`as/tests/safety.test.js:2135`: 바트=심화2·정규반1·2 /
  에리카=정규반1 공동). 강사 축을 칸 하나로는 못 세운다.

**ⓑ 「보기 쉽게」의 답은 코드에 있다 — 물어볼 것이 아니었다.**

`as/Code.js:3336` [v9.81] 주석 원문 — **「유호 07-31 "강사 「반」 탭 첫 화면(반 리스트)이 후지다"」**.
그 교정의 결과물이 지금도 서 있다(15·16열 `반카드요약`·`반몬스터이미지`):

- Description = `반몬스터 이름 · 👥 N명 · ⚔️ 보스 HP 214`(또는 `🏆 이번 주 보스 격파!`/`🏖️ 보스 휴식주`)
- Image = 반 몬스터 이미지(`classMonster` :3182)

🔑 **즉 「반을 보기 쉽게」 = 「반 목록을 만들라」가 아니라 «카드에 그 반의 지금이 한 줄 보이게 하라»다.**
🚫 밋밋한 목록으로 다시 내기 — 이미 한 번 「후지다」로 반려된 모양이다.
⚠ 「반이 한 자릿수」는 반만 맞다 — 전교는 **24반 384석이 확정 기준선**(`as/Code.js:1195`
`setupSchedule(4)` · 유호 08-01)이고, 한 자릿수인 것은 **한 강사의 담당 반**이다.

**ⓒ 검수자와 강사는 이미 갈려 있다 — 물리로도, 화면으로도. 강사는 검수 콘솔에 «못 들어간다».**

- `functions/review/index.ts:92` — `검수역할 = ['inspector','director']`. 같은 파일 :91 이
  「`teacher` 는 직원이지만 검수자가 아니다」로 적는다. **문서상 금지가 아니라 기계로 선 벽**이다.
- `functions/teach/index.ts:149` — `강사역할 = ['teacher','director']` · 경로표 7개. **강사 문은 이미 있다.**
- 머리말 :13 이 축까지 적어 뒀다 — **검수자 = 전사 축**(무엇을 말했나) · **강사 = 교정 축**(교정이 맞나).

## §2 🔴 급소 — `actor_kind='teacher'` 는 「강사」가 아니라 「사람 직원」이다

*(좌표 4줄 = `1a1b47ad` 실측 회수 · 폐기 스텁에서 가져왔다)*

- `review/index.ts:572` — 검수 확정이 `'teacher'::engine.actor_kind` 로 **INSERT 된다**. 그 값의 실제 뜻은
  「강사」가 아니라 **「사람 직원」**이고, c6 이 `actor_values` 를 못박아 검사하므로 값을 늘리는 것도 계약 변경이다.
- `review/index.ts:490` — `supersedes` 대상 판정이 `c.actor_kind='teacher'` **하나로만** 가른다 →
  강사 행이 정당한 재검수 대상이 되고, 거절 문구조차 사람 눈에 정상으로 보인다.
- `promotion_intent` CHECK(`= false or actor_kind='teacher'` · c9·c11) → 검수 안 거친 강사 한 마디가
  **훈련 승격 후보** 자리에 설 수 있다.
- ⚠ 더 센 가설(강사 행이 `submissions.transcript_verified` 를 덮는다)은 열어 보니 **틀렸다** —
  그 칸은 승인 트랜잭션이 직접 쓴다(`review/index.ts:582`). 검수 큐 이탈도 teacher 행 존재가 아니라
  `pipeline_jobs.status` 다. **셋만 실물이다.**

→ 🚫 **강사 피드백을 `corrections` 에 쓰지 않는다** · 🚫 `actor_kind` 에 값 추가(계약·CHECK·검사 셋이 동시에 흔들린다).
저장 자리는 §6.

## §3 「반」을 물리로 세운다 — c12 조각

```
engine.classes        class_id uuid pk · class_key text · season_id · display_name text
                      · active boolean not null default true · schema_ver
engine.learners       + class_id uuid references engine.classes(class_id)
engine.staff_classes  (staff_id, class_id) 복합 pk          -- 강사↔반 배정
```

- **`learners.class_name text` 한 칸으로 줄이면**: 시즌(교재 1권 ≈ 2달)마다 반이 재편되는 날 과거 행과
  계보가 끊긴다. `corrections` 에 이름 문자열 대신 `staff_id` 를 적기로 한 것과 **같은 이유**다.
- **`staff_classes` 를 빼면**: 「자기 반」이라는 말이 정의되지 않아 **모든 강사가 전교생 큐를 본다** —
  유호님 지시의 정반대다.
- 🔴 **PK 를 `class_key`(`평일11A`)로 두지 않는 이유 — 키가 시즌을 넘어 «재사용»된다.** 그 키는
  「평일 11시 A실」이라는 **좌표**이지 학생 무리의 정체성이 아니다. 다음 시즌 같은 슬롯에 다른
  무리가 앉으면 같은 키에 다른 반이 온다. 그래서 `class_id`(정체성) + `class_key`(좌표) + `season_id`
  **셋을 나눈다** — `engine.season` 은 이미 있다.
  ⚠ 대신 `class_key` 를 **버리지 않는다**: 그게 시트와 원본을 대조하는 유일한 자연키이고, 없으면
  다리가 어긋난 날 증상이 「조용함」뿐이다.
- ⚠ **`active` 없이 만들지 않는다** — 지난 시즌 반은 지우는 게 아니라 닫는다(과거 행 FK 가 살아야 한다).

**채우는 것도 사람 손이 아니다**

- `lib/명부규칙.js` 의 `열별칭` 에 **`반: ['class_name','반','클래스','class']` 한 줄**을 더한다.
  시트 5열이 그 순간부터 읽힌다.
- 🔑 **열이 전량 비면 무시, 쓰는데 그 줄만 비면 거절** — 기존 `role` 열 처방과 **같은 규칙을 그대로**
  쓴다(새 규칙 0). 규칙 정본이 하나인 것이 F269 의 처방이다(CLI 와 스윕이 같은 lib 을 공유).
- 🔴 **`roster-ingest` 는 지금 `on conflict do nothing` — 넣기만 하고 갱신을 안 한다.** 반은 바뀌는
  값이라 **`do update set class_id = excluded.class_id`** 가 필요하다. ⚠ 갱신 대상을 **그 한 칸으로
  한정**한다 — 전체 upsert 로 넓히면 시트 오타 한 번에 이름·연락처가 원본에서 덮인다.
- 🔴 **강사↔반 배정은 시트에 없다** — 「이 반을 누가 가르치나」가 어디에도 없다. 그건 원장 화면의
  입력 칸 하나가 맞다(반 수가 한 자릿수~24라 사람이 눌러도 ㉡ 위반이 아니다 — **판단이 필요한 자리**다).
- **배정 이력 표는 만들지 않는다.** 학생이 반을 옮기면 옛 제출도 새 반 큐로 따라간다 — 피드백은
  「지금 그 학생을 가르치는 강사」가 하는 것이라 그게 맞는 동작이다. 이력의 소비자는 **지금 0**이고
  「나중에 쓸 수 있는 모양」은 통과가 아니다(철학 ④). ⚠ 되돌리면 비싸므로 §8 판정 자리.

## §4 통로 — `teach` Fn 의 새 라우트 셋 (새 함수 0)

| 라우트 | 하는 일 |
|---|---|
| `GET feedback/classes` | 내 반 목록 + 반마다 「기다리는 것 n」 + 이번 주 조용한 학생 |
| `GET feedback/queue?class_id=` | 그 반의 기다리는 산출물 |
| `POST feedback/give` | 한 산출물에 한 마디 + 처분 |

✅ **셋 다 섰다** (2026-08-12 `local_df3c7c6d`).
🔴 **「기다리는 것」의 정의가 설계에 없었다 — 여기서 못박는다**: ①**AI 교정이 이미 났다**
(`corrections.actor_kind='ai'` — 강사가 반응할 대상이 있어야 큐에 선다. §7 자동 하강이 앞단에서
빼 준 것은 여기 자동으로 안 들어온다 · 같은 축이다) ∧ ②**강사 한 마디가 아직 없다**
(`teacher_notes` 가 상태의 정본 · 감사 원장이 아니다). 이 술어는 **함수 하나**(`기다림질의`)이고
카드의 셈과 목록이 그것을 같이 쓴다 — 각자 적으면 「● 4 인데 들어가면 3건」이 되고, 강사는 자기가
뭘 놓쳤는지 찾다 시간을 쓴다(`correct:140` 의 「대기 12인데 집히는 건 0」과 같은 처방).
🔑 **새 거절 코드 하나** — `NOTE_BY_OTHER`(409). 한 반에 강사 둘이 정상이라(§1 ⓐ) 같은 산출물에
남이 이미 한 마디를 남긴 자리가 실제로 난다. 개서는 **자기 것만** 열고, 남의 것은 조용히 덮지 않는다.

- 🔑 **경로표(`teach/index.ts:131`)에 등재하면 메서드·안내문이 파생된다** — 손으로 세 곳에 적지 않는다.
- 🔑 **권한은 라우트가 아니라 `staff_classes` 가 건다** — `staff_id` 로 조인해 자기 반만 나온다.
  `class_id` 를 인자로 받되 **믿지 않는다**(`service_role` 은 RLS 를 우회하므로 Fn 이 자기 손으로 한 번
  더 판정한다 — §4-3 ⚠ 기존 규칙).
- 🔑 **`staff_access_log` 에 그대로 적는다** — 「큐 응답 1회 = 1행」 규약을 따르고 `action` 값만 새로
  쓴다(`teach.feedback.queue`·`teach.feedback.give`). CHECK 가 없는 이유가 정확히 이것이다.

### §4-b 짓기 전 실측 — **이 통로는 §5 화면의 재료를 안 줬다** (2026-08-12 `local_62036356`)

`feedback/queue` 가 내던 것은 「누가·언제·무슨 과제」뿐이었다. §5 ③ 은 **「학생이 쓴 문장」**을 화면에
놓는데 그 문장이 응답에 없었다 — 학생이 낸 글도, AI 가 이미 낸 교정도 없이 「한 마디」를 쓰라는
화면은 **강사가 쓸 수 없는 화면**이고, 안 쓰이는 판정 칸의 생산량은 0이다(§8-3 이 통로를 세우고도
같은 이유로 0이었던 그 자리와 같은 모양).

- ✅ **다섯 칸을 더한다** — `body_original`·`transcript`·`ai_corrected_text`·`ai_explanation`·`ai_error_tags`.
- 🔑 **술어(`기다림질의`)에 매달지 않는다.** 그 함수는 카드의 셈과 목록이 같이 쓰는 하나뿐인 정의고,
  거기에 본문을 붙이면 카드 한 장 그리려고 **내 반 전체의 대기 글**을 끌어온다(화면 어디에도 안 쓰인다).
  술어가 «고른» id 로 한 번 더 훑는다 — 갈리는 것은 「무엇을 고르나」가 아니라 「고른 것의 무엇을
  싣나」라 정본은 그대로 하나다.
- 🔑 **AI 행이 여럿이면 가장 최근 것** — 규칙을 여기서 새로 정하지 않고 `review_queue` 의 lateral 을
  그대로 쓴다(`20260809050000_review_c10.sql:152`). 두 곳이 각자 정하면 검수 화면과 강사 화면이 다른
  문장을 보고, 증상은 「선생님이 본 교정이랑 다른데요」라 아무 데도 안 남는다.
- 🚫 **`model`·`prompt_ver` 는 안 싣는다** — 골든 큐가 안 싣는 것과 **같은 축**이다. 같은 사람이 이
  화면을 보고 그 주 골든을 판정하므로, 한쪽만 잠그면 잠근 쪽이 무의미해진다.
- 🚫 **학생글과 전사를 한 칸으로 접지 않는다**(`coalesce` 0) — 쓰기의 원문과 말하기의 「들린 대로」는
  확신의 결이 다르다. 접으면 화면이 기계가 «들은» 문장을 학생이 «쓴» 문장이라고 말하게 되고,
  강사는 그 오차를 학생의 실수로 읽는다.
- ⚠ 검수 판(`review_queue`)이 `body_original` 을 일부러 뺀 것(`20260807190000_review_c8.sql`)과 **갈리는
  자리**다: 그쪽 소비자는 이름을 못 보는 **검수자**이고, 이 응답은 이미 `display_name`·`student_code` 를
  싣는 **담임 강사**다. 같은 열이라도 받는 사람이 다르면 같은 판정이 아니다.

## §5 화면 — 반 카드 → 반 안 → 한 마디

```
[① 내 반 — 카드]              [② 3급 A반]                  [③ 바트 · 어제 숙제]
 ┌────────┐ ┌────────┐        오래 기다린 순               "..." 학생이 쓴 문장
 │ 3급 A반│ │ 3급 B반│        ─────────────                ─────────────────
 │ 👥16 ●4│ │ 👥15 ●1│        바트    2일 · 숙제           [한 마디 쓰기]
 └────────┘ └────────┘        체첵    1일 · 말하기          [확인] [다시 해보자]
 ⚪ 이번 주 한 번도 안 연: 투야 · 간바
```

- ✅ **카드다**(ⓑ) — 카드 한 장 = 반 하나, 그 반의 **지금**이 한 줄(`👥 N명 · ● 기다리는 것 n` /
  `○ 다 봤다`). 대기 건수가 카드에 있어야 강사가 **들어가기 전에** 어디부터 볼지 정한다 — Glide 판에서
  보스 HP 가 하던 자리다.
- 🔴 **정렬 축이 곧 비교다.** 반 안 정렬은 **「오래 기다린 순」 하나**다 — 점수·정답률·진도로 정렬하는
  순간 그 화면이 등수표가 되고 철학 ㉢(학생끼리 비교하지 않는다)을 정면으로 깬다. 기다린 시간은
  학생의 속성이 아니라 **내 일감의 나이**다. 🚫 평균·상위·배지·「다른 아이보다」 어느 자리에도.
- 🚫 **강사에게 전교생 큐를 기본값으로** 보이지 않는다. 담당 반 0개인 강사는 빈 화면이 아니라
  「배정된 반이 없습니다」를 본다 — 빈 화면과 권한 없음이 같은 모양이면 안 된다.
- 🎯 **한 수 더 — 「이번 주 한 번도 안 연 학생」**(`1a1b47ad` 안 · 채택): `staff_access_log.target_ids` 가
  **이미** 「누가 어느 학생 행을 열었나」를 쌓고 있다 → 뒤집으면 그 목록이 공짜로 나온다(새 표·새 수집 0).
  조용한 학생이 조용한 채로 지나가는 것을 기계가 막는다. 🚫 학생에게 보여주지 않는다(강사 자신의 행동 기록이다).
- 강사 탭 = 메뉴 **둘**(피드백 · 골든 판정). 🚫 **한 화면으로 합치지 않는다** — 합치면 골든의 주 5건
  상한이 반 큐 물량에 묻혀 「유인이 0인 업무」(M2 §4)가 영영 안 끝난다.
- 🚫 **골든 큐(N10·N11)에 반 축을 넣지 않는다** — M2 §4 의 주 5건 **전역 무작위**가 N12 성적표의
  분모다(M2 §5 「분모는 무작위 층만」). 반으로 층화하면 model·prompt_ver 승률이 **모델 성능이 아니라
  반 구성**을 잰다. 강사가 보는 큐가 둘이 되는 순간 「합치자」가 자연스러워 보이는 자리라 못박아 둔다.

## §6 무엇을 저장하나 — §2 때문에 열린 자리

`corrections` 는 막혔다(§2). 후보 둘:

- **㉠ 새 표 `engine.teacher_notes`**(submission_id · staff_id · body · created_at · schema_ver) —
  뜻이 섞이지 않고 CHECK·승격 사슬 어디에도 안 닿는다. 대신 표가 하나 는다.
- **㉡ 기존 사건으로** `learning_events`(`intervention.delivered` 계열) — 새 표 0인데 **강사 한 마디가
  학생에게 가는 개입**이라는 뜻이 이미 그 사건에 있다.

🎯 **도전안(통념: 「피드백 = 강사가 한 마디를 «쓴다»」) — 기본값을 «고르기»로 둔다.**
AI 가 이미 교정문+해설을 냈으므로 강사의 기본 동작은 빈 칸 타자가 아니라 **[이대로 보내기] /
[여기만 고치기] / [내가 쓰기]** 셋 중 하나를 누르는 것이다. 근거 = §5 의 「개원 뒤 사람 손이
실제로 도는 유일한 자리」라는 전제 그대로 — 손이 타자에서 클릭으로 내려가면 그 자리가 실제로 돈다.
⚠ **자기 반박**: 전부 고르기만 하면 학생이 받는 것이 결국 AI 문장이라 **「선생님이 봐 줬다」는
감각이 죽는다** — 그게 이 기능의 목적이었다. 그래서 **[내가 쓰기] 를 없애지 않고**, 한 줄 덧붙이기를
상시 열어 둔다. 🚫 고르기 전용으로 좁히기 · 🚫 이 도전안을 「AI 가 대신 피드백한다」로 읽기.
🔴 **미채택 — ⏳유호님 판정 자리**(§10-5): 강사의 말이 얼마나 들어가야 「선생님 피드백」인가는
취향·교육관이지 내가 정할 것이 아니다.

▶ **권고 = ㉠ 을 먼저, ㉡ 은 배달 배선이 설 때.** 근거 — 지금 필요한 것은 「강사가 쓴 것을 잃지 않는
것」이고, 배달(학생 화면에 어떻게 뜨나)은 아직 판정이 안 섰다. 배달 없이 ㉡ 을 쓰면 **소비자 없는
생산자**가 되어 그 자리에서 막힌다(도달 래칫).

✅ **㉠ 채택 · 섰다** (2026-08-12 `local_df3c7c6d` · 마이그 `20260812210000_teacher_notes_c11`).
🔴 **「계약 변경이라 혼자 안 정한다」는 전제가 틀렸다** — §8-1 이 이미 바로잡은 것과 **같은 이름
오류**다. 계약판을 올려야 하는지는 셋으로 잰다: 새 `event_type` · 새 값목록(`계약` 파일의 열거) ·
`learning_events` 손댐. `teacher_notes` 는 셋 다 **0**이라 c11 그대로다(`class_c11`·`season_c11`·
`season_review_c11` 이 이미 같은 판단으로 섰다). 그래서 §10-2 는 **유호님 자리가 아니었다** — 자율
판정으로 세우고 거부권을 남긴다(뒤집으면 마이그 한 벌 되돌림 · 행 0이라 지금은 비용이 0이다).
🔑 **㉠ 이 도달 래칫을 안 밟는 이유**(㉡ 과 갈리는 지점) — 이 표는 **태어나는 순간 소비자가 있다**:
강사 큐가 「한 마디가 이미 있는 것」을 빼는 데 쓴다. ㉡ 은 그 소비자가 배달 배선인데 그게 아직 없다.
🔑 **도전안(§10-5)이 ⏳인 채로도 물리는 섰다** — `origin` 칸(`as_is`·`edited`·`written`)을 지금 둔다.
답이 무엇이든 **화면 버튼 구성만** 갈리고, 안 두면 답 나는 날 이미 쌓인 행의 갈래를 영영 복원 못 한다.

## §7 G2 「맞음」 자동 하강 — 큐 «조회 시점»이 아니라 **AI 교정 앞단**에 선다

`955e8a16` 안은 「큐 조회 시점 재계산으로 `맞음` 을 내린다」였다(저장 0 · `is_correct` 안 만듦).
**저장 0 은 옳고 자리는 늦다.** 실측 넷:

| # | 실측 | 자리 |
|---|---|---|
| ① | AI 교정 대기조건에 **task 필터 0** — `body_original` 이 비어 있지만 않으면 무엇이든 벤더에 간다 | `functions/correct/index.ts:146` |
| ② | 골든 표본 **풀술어에도 필터 0** — `actor_kind='ai'` + 빈 카드 아님, 그뿐 | `lib/골든표본.js:47` |
| ③ | 검수 큐의 AI 조인은 **inner** — AI 행이 없으면 애초에 큐에 안 뜬다 | `review_queue`(`20260809050000`) |
| ④ | G2 짚음 갈래 = `submission.created`+숙제제출+`body_original`(**문장 전문**) | `lib/보고서교정제출.js:216·232` |

🔴 **늦게 내리면 이미 둘이 벌어져 있다.** ⑴ **과교정률 계기판이 거짓으로 부푼다** — 정답대로 고친
문장을 AI 에게 「고쳐 달라」고 보내면 무언가를 만들어 내고(①④), 그 행이 골든 풀에 든다(②).
뽑히면 강사가 「①원문이 이미 맞다 = 과교정」을 눌러, M2 §5 가 **「21/21≠100%」 실패모드를 상시
감시하려고** 세운 숫자가 **G2 정답 문장 때문에** 나빠진 것처럼 보인다. ⑵ **벤더 호출이 그만큼 돈**이다.

✅ **처방 = `correct` 의 `대기조건()` 에 술어 하나.** ③ 덕에 **새 표 0 · 새 열 0 · 새 상태값 0** —
AI 행이 안 생기면 inner 조인이 알아서 큐에서 뺀다.

- 🔑 **`대기조건()` 이 상수가 아니라 함수인 것이 여기서 값을 한다** — 그 조각은 「대기 수」와 「집는
  목록」 **두 질의에 같이** 끼워진다(`correct/index.ts:140` 이 「대기 12인데 집히는 건 0」을 막으려고
  그렇게 뒀다). 한 곳에 걸면 계기판과 실동작이 같이 움직인다. 🚫 목록 쪽에만 걸기.
- 🔴 **`pipeline_jobs.status` 를 새 값으로 옮기지 않는다** · 🚫 `discarded` 재사용(폐기 통계 오염).

**급소 = 판정이 두 벌이 되는 자리.** 판정 정본은 순수 함수 `lib/보고서교정.판정(선택자리, 교정문, 정답)`
인데 `대기조건()` 은 SQL 조각이다. SQL 에 손으로 다시 적으면 그게 두 번째 정본이고, 갈리는 날 증상은
「어떤 맞음은 걸러지고 어떤 맞음은 안 걸러진다」라 **아무 데도 안 남는다.**

- ✅ **처방이 이미 저장소에 있다(새 패턴 0)** — `lib/동의게이트.지금유효술어` · `lib/골든표본.풀술어`:
  **SQL 술어 «텍스트»를 lib 이 내보내고** 두 소비자가 그 하나를 끼운다(`골든표본.js:37` 원문 —
  「둘이 각자 적으면 그게 두 번째 정본이고, 갈리는 날 **분모와 분자가 다른 풀을 센다**」).
- **재현 가능한 근거 = 정규화가 정확히 셋뿐**(`보고서교정.js:106` §6-4 ② — 앞뒤 공백 제거 · 연속 공백
  1칸 · 종결 마침표 유무 무시). ⚠ 넷째를 얻는 날 술어도 같이 오른다 — 결속 회귀가 그걸 진다.
- 🚫 술어를 느슨하게 — 느슨하면 **안 고친 답까지 AI 를 건너뛰어** 진짜 오류가 아무에게도 안 간다
  (새는 방향이 「통과」인 자리라 좁게 연다 · 모르면 AI 로 보낸다 = 안전 방향).
- ⚠ **대조 문항은 이 술어의 대상이 아니다** — 「고칠 곳 없음」은 `quiz.answered` 라 submission 행 자체가
  없다(`보고서교정제출.js:252`).

**학생 쪽 ㉡ 서버 판정**(`955e8a16` 안 그대로 · 재판정 없음): `POST /v1/events` 응답이 이미
`{ok, results:[…]}` 사건별 배열이라 **칸 하나만 더한다 · 새 엔드포인트 0**. 피드백은 **앉음(3문장) 밖**
에서만 — 안에서 주면 2·3번의 `selected_option`·`latency_ms` 가 탐지 능력이 아니라 방금 받은 힌트에
대한 반응이 된다(재는 층이 값을 바꾼다).

**한 수 더**(유호 08-12 채택 · 미착수): 대조 문항의 **자리를 매일 결정적 셔플**(`prompt_seed` 기반 ·
무작위 ✗). 3장 중 대조가 늘 같은 자리면 학생이 자리를 외워 §9-③ 계기판이 거짓이 된다.

## §8 착수 순서

1. **반 원본 + 다리** — c12 마이그(표 2 + 칸 1) · `명부규칙` 열별칭 1줄 · `roster-ingest` **좁은 upsert** · 회귀.
2. **G2 「맞음」 술어** — `lib/보고서교정.맞음술어` 신설 + 결속 회귀(같은 픽스처를 `판정()` 과 SQL
   양쪽에) → `correct` 대기조건에 끼움 + 회귀 셋(같은 답 안 집힘 · 다른 답 **집힘** · 발화녹음 영향 0).
   **1번과 독립이라 병행 가능.**
3. ✅ **`teach` 라우트 셋**(§4) — `staff_classes` 조인이 권한을 진다.
   *(2026-08-12 `local_df3c7c6d` · 마이그 `20260812210000_teacher_notes_c11` + `lib/반피드백.js`
   + 라우트 셋 + 회귀 24 · 전량 1748/1748. 🔴 **라이브 붓기·재배포는 ⏳유호님 승인**(§10-4)이라
   지금 초록은 **파일 층**이다 — 원격 적용 0 · 실왕복 0.)*
4. ✅ **화면**(§5) — 반 카드 그리드 → 반 큐 → 한 마디.
   *(2026-08-12 `local_62036356` · `src/반피드백화면.js`+`src/반피드백API.js` 신설 · `App.js`·
   `도착확인` 진입 줄 · 회귀 34 · 변이 12/12 · 전량 1813/0. 🔴 **§4 가 화면의 재료를 안 줬다** —
   그 자리를 같이 닫았다: §4 아래 「짓기 전 실측」 절. 🔴 라이브 붓기·재배포는 여전히 ⏳유호님
   승인(§10-4)이라 지금 초록은 **파일 층**이다 — 원격 적용 0 · 실왕복 0.)*
5. ✅ **리허설 실왕복** — 인수 조건 **둘**: ⓐ담당 반 **밖** 학생이 **0행**으로 막히는 것을 일부러 심어
   증명(「내 반만 보인다」를 화면 규약으로 두면 서버가 안 지킨다) ⓑ「맞음」 제출을 심고 **AI 행 0 ·
   검수 큐 0행**(술어만 회귀로 보면 조인 ③ 이 정말 빼 주는지는 안 재진다).
   *(2026-08-15 `local_47d08bd3` · `tools/반피드백왕복시험.js` **27/27** · 리허설 실왕복.
   ⓐ 는 배정 한 줄을 **붙였다 떼서** 같은 응답이 뒤집히는 것까지 봤다 — 그래야 0행의 사유가
   「관문」인지 「빈 데이터」인지 갈린다. ⓑ 는 「맞음」을 **정확 일치**와 **공백·마침표만 다름**
   두 벌로 심어 `정규화식` 이 DB 에서 실제로 도는 것을 처음 쟀다. 자세한 것은 아래 §8-5 절.)*

## §9 「똑똑하게」 게이트 (배관이라 ③④를 진다)

- ③ **운영** — 반 배정은 이미 사람이 하는 확정 하나이고 이 설계는 그 손을 **안 늘린다**(열별칭 1줄로
  이미 오는 것을 받는다). 자동 하강은 사람 손을 **줄인다**. 늘어나는 손 = **강사↔반 배정 입력 하나**
  (판단이 필요한 자리라 ㉡ 위반이 아니다).
- ④ **완결 3칸** — 모였나 **✓** · 닿았나 **✗→○**(다리가 §8-1) · 늘었나 **✗** 🔴 **정직하게**: 이 설계는
  대장의 새 칸을 채우지 않는다. 채우는 것은 G2 자체(㉡ 사람 이해)이고 여기는 **그 재료가 사람 손에
  막히지 않게 하는 배관** + **거짓 신호를 빼는 일**(과교정률이 진짜 과교정만 세게 된다).
  ⚠ 단독으로 「엔진이 자랐다」고 보고하지 않는다.

## §10 혼자 정하지 않는 것 (⏳유호님 · 답 없이도 §8-1·2 는 선다)

1. **강사 계정이 실재하나** — `engine.staff` 에 `teacher` 행이 지금 몇인지. 0이면 §8-4 는 **볼 사람이
   0명**이라 리허설까지가 상한이다.
2. ~~**§6 저장 자리** ㉠새 표 / ㉡기존 사건 — 계약 변경이라 올린다(권고 = ㉠).~~
   ✅ **철회 — 유호님 자리가 아니었다**(§6 참조). 「계약 변경」이라는 전제가 §8-1 이 이미 바로잡은
   것과 같은 이름 오류였다(새 `event_type` 0 · 새 값목록 0 · `learning_events` 0 = c11 그대로).
   ㉠ 으로 세웠고 **거부권을 남긴다** — 행이 0이라 지금 되돌리는 비용은 0이다.
3. **반 배정 이력**(§3 마지막) — 지금 아니면 비싸진다.
4. **운영 배포** — 라이브라 유호님 승인 자리(리허설까지가 세션 몫).
5. **§6 도전안 — 피드백 기본 동작을 「쓰기」로 둘지 「고르기」로 둘지.** 강사의 말이 얼마나 들어가야
   「선생님 피드백」인가는 취향·교육관이라 내가 안 정한다. 답 없이도 §8-1~3 은 선다(저장 자리와
   화면 버튼 구성만 갈린다).

## §11 하지 않는 것

- 🚫 반을 화면 문자열로만 · 🚫 `roster-ingest` 전체 upsert · 🚫 사람이 반을 다시 입력하는 칸 · 🚫 배정 이력 표(지금).
- 🚫 `review` 문에 `teacher` 넣기(M2 §2 · `검수왕복시험:230` 핀) · 🚫 강사 피드백을 `corrections` 에 쓰기 ·
  🚫 `actor_kind` 에 값 추가 · 🚫 검수 콘솔에 강사 메뉴 얹기.
- 🚫 반 목록을 밋밋한 목록으로 · 🚫 전교생 큐 기본값 · 🚫 학생 간 비교(정렬 축 포함) · 🚫 골든 큐에 반 축 ·
  🚫 강사 탭 두 메뉴 합치기.
- 🚫 하강을 조회 시점에 · 🚫 `is_correct` 저장 · 🚫 `pipeline_jobs` 새 상태값 · 🚫 SQL 에 판정 손으로 다시 적기 ·
  🚫 술어 느슨하게 · 🚫 대조 문항 갈래를 술어가 건드리기 · 🚫 앉음 안에서 즉시 피드백.

## §12 검증 이력

- **v1** 2026-08-12 (`local_5c3f51e1` · `e8b0631`) — 실측 ⓐⓑⓒ 위에 세움. 자기 반박이 잡은 것 둘:
  ①「반 축을 큐에」를 골든 큐로 읽으면 N12 분모가 깨진다 → 두 큐로 갈랐다 ②판정이 JS 순수 함수인데
  대기조건은 SQL 조각이라 두 벌이 된다 → 술어 텍스트 선례 둘을 찾아 세웠다.
- **범위 축소 → 되돌림** 같은 날 (`7f64606` → 이 판) — **F381 의 두 번째 마디**. `1a1b47ad` 가 같은 🎫 로
  `c7ed0d14` 를 먼저 냈기에 내가 UX 절반을 넘겼는데, 그 세션이 근거 넷을 대고 **반대로 나에게 넘겼다**
  (그쪽 ⓑ 가 틀렸다 · 이쪽이 🎫 전부를 덮는다 · 정본이 코드 옆에 산다 · §7 추정을 이쪽이 실측으로 뒤집었다).
  **서로 비켜서서 UX 절반이 주인 없이 뜬 것이 진짜 사고다** — v2 가 🎫 전부를 여기서 지고 닫는다.
  🔑 **가져온 것**: 그쪽 ⓐ 의 열별칭 실측(내 것보다 정확했다 — 나는 「다리가 없다」까지, 그쪽은
  「이미 오는데 버린다」까지) · §2 좌표 4줄 · §3 3표 · §4 라우트·감사 · §5 정렬 축·한 수 더.
  🔑 **바꾼 것**: PK 를 uuid 로 두되 `class_key`·`season_id` 를 **함께** 둔다(그쪽 우려의 뿌리는 이름이
  아니라 **키의 시즌 재사용**이었다) · ⓑ 기본값을 목록→**카드**로.
- **§8-4 구현(화면)** 2026-08-12 (`local_62036356`) — 짓기 전 실측이 **§4 의 구멍**을 냈다: 통로가
  §5 ③ 이 요구한 「학생이 쓴 문장」을 안 줬다(→ §4-b). 그밖에 잡아 고친 것 셋:
  1. 🔴 **회귀가 자기 주석을 위반으로 잡았다** — 「화면에 «평균»이 있나」 검사가 *금지를 설명하는
     JSX 주석*에 걸렸다. `tests/lib/소스검사.js` 의 제거기가 `{/* … */}` **여러 줄**을 못 지웠기
     때문이고, 저장소의 화면 주석은 전부 그 표기다. 거짓양성이라 눈에 띄었지만 **반대 방향
     (설명이 코드로 읽혀 영원히 초록)이 그 파일 머리말의 실측 그대로**라 같은 구멍의 다른
     얼굴이다 → 제거기를 고치고 픽스처에 그 모양을 심었다(「평균」을 일부러 넣은 JSX 블록).
  2. 🔴 **변이 12 중 2 가 통과했다**(1차 실측) — ⓐ 「AI 교정 열을 select 에서 뺀다」가 초록이었다:
     응답 조립부의 같은 이름이 검사를 통과시켰고, 그때 증상은 오류가 아니라 **전 항목 null**이라
     아무 데도 안 빨개진다 → 질의와 응답을 **갈라** 잰다. ⓑ 「기다린 날을 자정 경계로」가
     초록이었다: 픽스처가 «같은 날 오전»이라 경계를 안 밟았다 → 「어제 23시 ↔ 오늘 09시」로
     바꿨다. **2차 12/12.**
  3. 🔑 **재료 다섯 칸을 `재료칸()` 헬퍼로 뺀 것은 가드가 시킨 모양이다.** 처음엔 항목 조립을
     블록 본문으로 펴서 `items: 항목들.map((r) => ({` 한 줄을 지웠는데, `git-scope-guard` 가
     「남이 방금 커밋한 줄을 지운다」로 막았다(§8-3 이 그 줄의 주인이다). 우회하지 않고
     **그 줄이 살아남는 모양**으로 다시 지었다 — 결과가 더 낫다: 재료 행이 없을 때의 기본값
     (`null` 다섯)이 **한 자리**에 서서, 호출부가 늘어도 「그 칸만 `undefined` 로 사라지는」
     갈래가 안 생긴다. 대신 사슬이 둘이 되므로 회귀도 둘을 본다(헬퍼가 만드나 · 항목이 펴나).
  4. 🔑 **갈래(`origin`) 세 값이 화면에서 도달 가능해야 한다** — §6 도전안이 ⏳인 채로 [내가 쓰기]만
     두면 `as_is`·`edited` 는 DB 에 영원히 안 쌓이고, 답이 나는 날 그 사이 행의 갈래를 복원 못
     한다(그 칸을 «지금» 둔 이유가 그것이었는데 화면이 그 뜻을 못 살리는 자리였다). 기본은
     쓰기로 두되 **「AI 교정 그대로 넣기」 한 줄**을 열어 갈래를 «동작»에서 낸다 — 유호님 답이
     갈리는 것은 「어느 것이 기본 버튼인가」뿐이다. 회귀가 세 값 도달을 못박는다.
- **§8-1 구현** 2026-08-12 (`local_c84a3b8a` · 마이그 `20260812200000_class_c11.sql`) — 짓다가 **설계가
  틀린 자리 둘**을 만났다. 문서를 안 고치면 다음 세션이 문서를 믿으므로 여기 박는다.
  1. 🔴 **「c12 조각」은 이름 오류다 — c11 로 지었다.** 네 Edge Function(auth·events·uploads·deliver)이
     계약판을 **최신 마이그 파일 이름**에서 읽고(`tests/마이그레이션이름.test.js`), 제약 이름의 접미는
     `계약/수집_교정_계약.json` 의 `버전`(지금 `c11`)에서 파생된다(`tests/L0스키마.test.js` 의 `제약()`).
     이 조각은 계약을 안 바꾼다(새 `event_type` 0 · 값목록 0 · `learning_events` 0). `season_c11`·
     `season_review_c11`·`learner_profile_c11` 셋이 `engine_c11` 뒤에 c11 을 이어 쓴 것과 같다.
  2. 🔴 **「`역할` 열 처방과 같은 규칙」(§3)을 그대로 안 썼다 — 빈 반은 거절이 아니라 «셈»이다.**
     두 열이 재는 것이 다르다. `역할` 은 *이 줄이 학생인가*를 정해서 모르면 답이 없지만, `반` 은
     *이미 학생인 줄의 속성*이다. 거절하면 스윕이 그 행을 흘려(거절 단위가 행이다) 그 학생이
     `engine.learners` 에 **아예 안 서고**, 반 배정이 앱 접속의 선행 조건이 된다 — 막으려던 것
     (강사 큐에서 조용히 빠짐)보다 나쁘고, 사람이 칸을 채워야 제품이 도는 모양이라 철학 ㉡ 에도
     어긋난다. 그래서 `이름` 쪽 선례를 따라 null 로 넣고 `반미배정` 으로 **매 판 이름을 부른다**.
     「열이 전량 비면 무시」는 그대로다. 회귀 = `tests/반배정.test.js`.
  🔑 **설계가 안 정해 둬서 구현이 정한 것 셋**(뒤집으려면 여기부터 본다):
  ㉠ `engine.classes` 는 **명부 스윕이 자동 생성**한다(시트 좌표 → `class_key`·`display_name` 같은 값).
  ㉡ 매다는 시즌 = `ends_on is null` 인 열린 시즌, 없으면 null(「시즌 밖」 = 미개원 지금).
  ㉢ 유일성은 제약이 아니라 **부분 인덱스 둘**이다 — 유일 제약은 NULL 을 서로 다르게 보므로
     `season_id is null` 인 지금 상태에서 같은 좌표가 몇 벌이든 들어온다.
  🔴 **아직 없는 것**: 시즌이 넘어갈 때 지난 시즌 반을 `active=false` 로 닫는 일감(시즌 롤오버)이
     어디에도 없다. 첫 시즌이 끝나기 전에 서야 하고, 안 서면 반 목록이 시즌마다 누적된다.
- **§8-2 구현** 2026-08-12 (`local_df2ac398` · `0be2fae`) — 술어는 `lib/보고서교정.맞음술어`,
  소비자는 `correct` 의 `대기조건()`, 결속은 `tests/보고서교정.test.js` ⑨ 절(회귀 7). talk 1724/1724.
  짓다가 **설계가 못 본 자리 하나**를 밟았다. 문서를 안 고치면 다음 세션이 문서를 믿으므로 박는다.
  1. 🔴 **「정규화가 정확히 셋뿐」은 안전의 근거로 부족하다 — 텍스트가 층을 «셋» 지난다.**
     §7 은 재현 가능한 근거로 정규화 3종을 댔고 그것은 맞다. 그런데 술어 텍스트는
     `lib` 의 **JS 문자열** → `correct` 의 **TS 템플릿 리터럴** → **SQL 문자열 리터럴** 을 지나고,
     가운데 층에서 `\s` 는 «정의되지 않은 이스케이프»라 조용히 `s` 로 접힌다. 그러면 SQL 에
     `'s+'` 가 박혀 **「공백 정규화」가 「s 정규화」가 된다** — 문법 오류가 아니라서 증상은
     「어떤 맞음은 안 걸러진다」뿐이고, 그건 §7 이 급소로 지목한 바로 그 모양이다(판정 두 벌).
     처방 = **백슬래시를 한 개도 안 쓴다**: `\s`→`[[:space:]]` · `\.`→`[.]`. POSIX 문자 클래스는
     세 층 어디서도 뜻이 안 바뀐다. 회귀가 lib·소비자 양쪽에서 백슬래시 0 을 센다.
  🔑 **설계가 안 정해 둬서 구현이 정한 것 셋**(뒤집으려면 여기부터 본다):
  ㉠ 술어는 **표 하나에 못 산다** — 자리는 `e.payload ->> 'selected_option'`, 글·정답은
     `s.task_snapshot` 이라 별칭 `s`·`e` 둘을 전제한다(`대기조건()` 이 이미 그 이름으로 조인한다).
  ㉡ **`판정()` ⓐ(대조 문항 「고칠 곳 없음」)를 술어에 안 적었다** — 그 갈래는 `quiz.answered` 라
     `submissions` 행 자체가 없다. 적으면 **영영 거짓인 마디**가 늘고 읽는 사람은 그것이 무언가를
     막는다고 믿는다. 그래서 두 층의 「맞음」이 같은 말이 아님을 회귀가 따로 못박는다.
  ㉢ 술어는 `body_original` «만» 본다(대기조건 본체의 `coalesce` 와 일부러 다르다) — 발화 녹음이
     이 술어에 걸릴 길을 아예 없앤다.
- **§8-3 구현** 2026-08-12 (`local_df3c7c6d` · 마이그 `20260812210000_teacher_notes_c11` ·
  `lib/반피드백.js` · `teach` 라우트 셋 · 회귀 24 = `tests/반피드백.test.js` · talk 1748/1748).
  짓다가 **설계가 틀렸거나 안 정해 둔 자리 넷**을 만났다. 문서를 안 고치면 다음 세션이 문서를 믿는다.
  1. 🔴 **§10-2 는 유호님 자리가 아니었다**(§6 에 상술) — 「계약 변경」이라는 전제가 §8-1 이 이미
     바로잡은 것과 **같은 이름 오류**다. 셋으로 재면(새 `event_type` 0 · 새 값목록 0 ·
     `learning_events` 0) c11 그대로다. ㉠ 으로 세우고 **거부권을 남긴다**(행 0 = 되돌림 비용 0).
  2. 🔴 **「기다리는 것」의 정의가 설계 어디에도 없었다.** §4 표가 「그 반의 기다리는 산출물」이라고만
     적었는데, 그 말이 안 정해지면 카드의 셈과 목록이 **각자** 정한다. §4 에 못박고 술어를 함수
     하나(`기다림질의`)로 두었다 — 회귀가 그 술어가 소스에 **정확히 한 번** 나오는지 센다.
  3. 🔴 **§5 「한 수 더」의 재는 층이 목적과 어긋났다.** 설계는 「이번 주 한 번도 안 연 학생」을
     `staff_access_log.target_ids` 에서 뽑자고 했는데, 세워 보니 **큐를 한 번 열면 그 반 학생이
     전원 「열림」**이 되어 그 목록이 영구히 빈다. 그래서 **한 마디를 줬나**로 잰다 — 화면을 연 것은
     주의가 아니고 말을 건 것이 주의다. 곁들여 **아무것도 안 낸 학생도 뜬다**(낼 것이 없으니 한 마디도
     없다): 조용한 학생을 드러내는 것이 이 칸의 목적이라 그게 맞는 동작이다.
  4. 🔴 **한 반에 강사 둘이 정상인데(§1 ⓐ) 개서 규칙이 없었다.** 개서를 열되(F103) **자기 것만**
     열고, 남의 한 마디는 `NOTE_BY_OTHER`(409)로 막는다. ⚠ `on conflict do update ... where` 는
     조건이 안 맞으면 **0행을 조용히 돌려준다** — 그 침묵을 「성공」으로 읽으면 저장이 안 되고도
     화면은 초록이다(회귀가 그 자리를 못박는다).
  🔑 **설계가 안 정해 둬서 구현이 정한 것 셋**(뒤집으려면 여기부터 본다):
  ㉠ 큐에 서려면 **AI 교정이 이미 나 있어야** 한다 — 반응할 대상이 없는 행을 강사에게 안 보낸다.
  ㉡ `target_ids` 의 단위는 라우트마다 다르다(classes=반 · queue=학생 · give=산출물) — 그 라우트가
     «무엇에 대해» 일했는지를 적고, 단위는 `action` 이름이 들고 있다(기존 두 라우트도 이미 그렇다).
  ㉢ 「이번 주」는 골든표본의 **ISO 주**를 그대로 쓴다 — 시간대 리터럴을 재기입하지 않는다.
  🔴 **아직 없는 것**(08-12 시점): ①§8-4 화면(주인 없음) ②원격 붓기·`teach` 재배포(⏳유호님 §10-4)
     ③실왕복 0 — 지금 초록은 **파일 층**이다. ④`engine.staff_classes` 를 채우는 원장 화면이 아직
     없어, 부어도 모든 강사가 「배정된 반이 없습니다」를 본다(§3 마지막 · 그 입력은 판단 자리라
     사람 손이 맞다).
     ✅ **08-15 갱신**: ①②③ 은 닫혔다(①=`ff5de3a` · ②=마이그 적용 실측 + `teach` v12 재배포 ·
     ③=`반피드백왕복시험` 27/27). **④는 그대로 열려 있다** — 원장 화면이 없으니 실학생이 오는
     날 반 배정은 여전히 손이나 SQL 이다.
  🔴 **안 잰 층**(08-12 시점): SQL 이 실제로 그 행을 빼는지는 DB 가 필요하다 — **§8-5 리허설 몫**이고
     이 커밋은 그 층을 「쟀다」고 주장하지 않는다. 잰 것은 텍스트 결속·갈래 대응·이스케이프뿐이고,
     탐지력은 소비자 쪽을 일부러 어긋내 2회 실측했다(한 글자 축약 → 결속이 욺 · `[[:space:]]` 를
     `\s` 로 되돌림 → 결속과 이스케이프가 함께 욺 · 둘 다 되돌렸다).
     ✅ **08-15 에 쟀다** — 그 층이 이 문서에서 가장 오래 비어 있던 칸이었다. 아래 §8-5 절.
  ⚠ `correct` 는 **재배포해야 실제로 돈다** — 커밋만으로는 라이브가 안 바뀐다(§10-4 승인 자리).
- **§8-5 실왕복** 2026-08-15 (`local_47d08bd3` · `tools/반피드백왕복시험.js` 27/27 · 전량 2189/2189)
  — 인수 조건 둘을 리허설 DB·함수 왕복으로 닫았다. **차단자로 적혀 있던 것이 셋 다 낡아 있었다**:
  마이그 `20260812210000` 은 리허설·운영 **양쪽에 이미 적용**돼 있었고(실측), 승인 축도
  08-12 유호 원문(운영 쓰기 자율)으로 낡았고, 남은 하나(`teach` 배포본이 2모듈 낡음)는
  리허설 재배포 한 번(v12)으로 닫혔다 — 그 낡음은 **남의 왕복시험도 막고 있었다**(F313 재발).
  1. 🔑 **0행은 사유를 못 가른다 — 그래서 «되돌리는 변이»를 시험 안에 넣었다.**
     ⓐ 를 「Y 가 안 보인다」로만 재면 관문이 막은 것과 볼 게 없는 것이 같은 초록이다. 배정
     한 줄을 붙이면 카드·큐가 **나타나고** 떼면 **다시 404** — 그러고 나서야 0의 사유가 관문
     하나로 좁혀진다. 배정된 동안 `feedback/give` 는 안 부른다(지울 수 없는 `teacher_notes` 가
     남의 반 학생에게 남고, 그 행이 다음 회차의 큐 판정을 조용히 바꾼다).
  2. 🔑 **검수 큐 0행도 사유가 둘이다** — 뷰 소속 조건이 「AI 교정 있음」과 「`status='ai_processed'`」
     둘이라, 그냥 0을 보면 status 때문일 수 있다. 「맞음」의 job 을 **일부러 `ai_processed` 로
     올려 두고도** 0행인 것을 잰다. 그래야 사유가 「AI 행 0」 하나로 남는다.
  3. 🔴 **생산자 Fn 을 측정 도구로 쓰면 안 된다**(F452 · 이번에 밟았다). `POST functions/v1/correct`
     는 분모를 돌려주지만 키가 있으면 **그 한 번에 대기 전량을 벤더 배치로 제출한다**. 측정
     5회가 곧 제출 시도 5회였다(그날은 벤더가 400 으로 튕겨 배치 0 · 과금 0 — 그건 운이지
     설계가 아니다). 지금은 **생산자 소스에서 `대기조건` 원문을 꺼내 DB 에 그대로 태운다**.
  4. 🔴 **곁들여 나온 것 — 리허설 `correct` 에 벤더 키가 살아 있고, 배치 제출이 400 으로 튕긴다.**
     응답은 **HTTP 200** 에 `{이유:'batch_submit_failed', status:400, 회수:{배치:0}}` 다. 즉 cron 이
     켜지는 날 이 통로는 **초록 얼굴로 아무것도 못 만든다**. 이 문서의 트랙이 아니라 「AI 교정
     생산자」 몫이라 여기서 고치지 않고 대기열에 세웠다(대기 396건이 그 앞에 서 있다).
  5. 🔑 **픽스처는 멱등이다** — `learning_events`·`corrections`·`consents`·`teacher_notes` 는 삭제가
     트리거로 막혀 있어, 회차마다 심으면 리허설이 영원히 자란다. 고정 키로 재사용하고, 심는
     모양을 고칠 땐 키의 **세대**를 올린다(옛 행은 update 가 막혀 못 고친다 — 안 올리면 새 코드가
     그 DB 에서 영영 안 돈다. 실제로 `consent_id` 스탬프를 더할 때 이 자리를 밟았다).
  ⚠ **안 한 것**: 운영(라이브) 왕복 0 — 이 시험은 리허설 전용이다. 그리고 §8-4 화면은 이 왕복이
     지나지 않는다(서버 세 라우트만 지난다).
- ⏳ **적대 심문 0회** — 컨텍스트 독립 검증자를 안 지났다(자기 반박 + 세션 간 교차 반박만 돌았다).
