# 종료된 후보 실험 — 액션 학습 통합 검증 (2026-09-12)

> **제품 적용 철회 · 후보 실험 종료.** 사용자가 문장 활쏘기 제품을 거절하고 **게임 실행부의 런타임 시험만** 진행하도록 범위를 줄였다. 아래는 철회 이전의 실험·검증 이력이며 현재 제품 계획·활성 기능·배포 완료의 근거가 아니다.
>
> 사용자 범위 축소에 따라 이번 문항팩·학습 조립기·API 연결·전용 시험을 삭제하고, 기존 계약·스냅샷 등록부·FSRS 파생·events/progress·관련 시험에서 이번 학습 추가만 걷었다. Postgres.js `3.4.9` 최신화와 기존 다른 작업은 유지했다. 커밋·push·배포·운영 DB 쓰기는 하지 않았다.
>
> 중단 직전 보안 보강은 events의 회차 잠금/충돌 처리와 서버 수신시각 정렬 코드까지만 작성됐고 동시성 대역·시험·오류계약은 미완성이었다. 그 미완성 추가도 이번에 함께 걷었다. 아래 208/85/18 등의 통과 수를 해당 미완성 보안 수리나 현재 활성 제품의 검증으로 확대하지 않는다.

철회 후 확인: 지정한 기존 11파일 중 8파일은 기준 HEAD `7d3cd56c538ad2c3dcea726f1ddad283f4d5ef5d`와 다시 동일하고, events/progress/progress 확인카드 시험 3파일은 Postgres.js `3.4.9` 한 줄 변경만 남았다. 기존 계약·게임 큐·스냅샷·복습 파생/계산기·progress·동봉/배포 묶음·구문 관련 **227/227 시험 통과**. 전체 앱 UI 정리와 전체 시험은 root 통합 확인 범위다.

## 확인한 범위

- 작업본: `C:/Users/q1212/Documents/SYNK-talk`.
- 전체 시험은 `rg --files tests -g '*.test.js' -g '*.test.mjs' -g '*.test.cjs'`의 **272개 파일을 명시**해 실행했다. `node --test`의 임시 폴더 자동 수집은 사용하지 않았다.
- Postgres.js 서버 import를 `3.4.9`로 바꾼 후, 테스트 대역의 옛 `3.4.4` 이름 6곳을 맞췄다. 실 서버 함수·인증/철회/저장 보호 단언을 제거하지 않았다.
- 첫 전체 측정: **3,506 tests / 3,488 pass / 16 fail / 1 skip / 1 todo**, 약 47.3초. 이것은 아래 후속 수정 전 스냅샷이며 현재 전체 통과 주장에 사용하면 안 된다.
- 별도 관련 시험: driver 대역 5파일 **85/85 통과**. 브라우저 전역 범위·복습 consumer 명부·progress 확인카드 후속 수정 **18/18 통과**.
- 액션 학습 원천 구현 시험은 실제 events/progress 핸들러 소스에 합성 Auth/DB를 주입했다. **실제 DB·실기기·배포·학생 성과 시험이 아니다.**

## 첫 전체 측정의 실패와 처리 구분

| 테스트 파일·사례 | 실제 원인 | 이번 담당 처리 |
|---|---|---|
| `tests/감사회귀_W2C4.test.js:70` — G3-6 App.js 겉테 고정 치수 | `겉테글` 상한 prop 사용처가 기존 기대 3곳에서 새 버튼 포함 4곳 | root가 실제 UI 확인 후 처리. 숫자만 완화하지 않음 |
| `tests/강사지식.test.js:174` — 생성물이 원본과 같다 | 형제 저장소 정본과 강사지식 생성물이 다름 | 기존 생성물 드리프트로 분리. 재생성하지 않음 |
| `tests/구문검사.test.js:100` — 추적 소스 전량 파싱 | `git ls-files`가 삭제 예정 `tools/스키아패치.js`를 아직 내놓아 ENOENT. 읽은 파일 582/583 | 삭제를 복원하거나 검사 제외하지 않음. 정확 범위 커밋 뒤 재검사 필요 |
| `tests/깊이.test.js:126` — 정본 수식 드리프트 | 몽글 311번 값 `0.64`와 정본 `0.639` 불일치 | 기존 자산 드리프트로 분리. 자산을 임의 재생성하지 않음 |
| `tests/미정의심볼.test.js:196` — 앱의 미정의 참조 | 새 `.web.js`의 정상 `window`/`document`를 네이티브 전역 목록이 모름 | **수정·통과**. `.web.js`에만 DOM 전역 허용. native/shared에서 계속 거절하는 별도 시험 추가 |
| `tests/복습스케줄.test.js:130` — 소비자 명부 | 새 `tests/액션학습.test.js`가 실제 계산기를 소비하지만 명부에 없음 | **수정·통과**. 해당 시험 1곳만 명부에 추가; 실물 대조 유지 |
| `tests/소스검사통로.test.js:310` — 원문 낱말 단언 래칫 | `tests/dependency-upgrade.test.js`가 기존 소스검사 통로를 사용하지 않음 | modernize 담당에게 전달. 래칫 상한 완화하지 않음 |
| `tests/스폰통로.test.js:172` — 테스트 자식 실행 통로 | `tests/교수장면자산.test.js:71` 직접 spawn | 기존 별도 작업과 분리. 직접 수정하지 않음 |
| `tests/원격배포.test.js` 아래 6사례 | 새 동봉 `lib/게임학습.js`가 작업본에는 있지만 HEAD에는 없음 | 커밋 전 배포 보호 동작. 작업본 배포로 우회하거나 검사를 무시하지 않음 |
| `tests/플래그게이트.test.js:155` — 게이트 없는 도구 명부 | `dependency-upgrade-audit.js`, `가이드장면자산반입.js`, `가이드장면표정반입.js`가 명부/통로와 어긋남 | dependency 도구는 modernize 담당. 기존 자산 도구 2개는 별도 변경으로 분리 |
| `tests/플래그게이트.test.js:169` — 명부 갈래 대조 | 위 세 도구 분류가 명부에 없음 | 같은 원인의 두 번째 단언. 게이트나 검사 제거하지 않음 |

원격배포의 여섯 사례:

1. `:100` 동봉 — CJS 검증기가 ESM으로 감싸여 실제로 import된다.
2. `:106` 동봉 — 계약 JSON이 ESM으로 감싸여 값목록을 그대로 낸다.
3. `:112` 동봉 — 감싼 둘을 붙이면 계약 검증이 실제로 돈다.
4. `:139` 동봉 — 배포되는 것은 작업본이 아니라 HEAD다.
5. `:188` 나갈 것 — 본체가 묶음에 들고 본체도 파일검사를 지난다.
6. `:208` 나갈 것 — 동봉도 주입된 파일검사를 지난다.

## 이번 통합 보수의 파일 소유 경계

- `tests/progress확인카드.test.js`: driver 이름 1곳, 실제 `게임학습` 모듈 대역 등록, 액션 SQL 분기(인증 학생 필터·500건 상한 확인, 합성 액션 행 0건) 추가.
- `tests/전사보호.test.js`, `tests/컴패니언통로.test.js`, `tests/학생기록.test.js`: driver 이름 각 1줄만 수정.
- `tests/교정자동화보호.test.js`: **작업 시작부터 untracked였던 다른 작업 원본**. 이 담당 변경은 driver 이름 **2줄뿐**이다. 파일 전체를 이 담당 신규 작업으로 취급하면 안 된다.
- `tests/미정의심볼.test.js`: web 전용 전역 범위 분리와 native/shared 거절 시험.
- `tests/복습스케줄.test.js`: 신규 합성 시험 consumer 1곳 등재.
- 기존 dirty `교정배치`, `조용한실패`, `처리장부`, `회차장부` 시험은 수정하지 않았다.
- `git diff --check` 확인. 커밋·push·원격 배포·원격 DB 변경은 수행하지 않았다.

## 남은 확인

root의 정확 범위 커밋과 UI/최신화 담당 수정 이후 **전체 명시 파일 시험 재실행**이 필요하다.
현재 문항팩은 사람 검수 전 `검수확정=false`이며, 기기 연습만 가능하고 운영 학습 수집은 닫혀 있다.
검수 완료·실제 학생 기록·원격 DB 왕복·실기기 성능을 이 문서로 완료 처리하지 않는다.
