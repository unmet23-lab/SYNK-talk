# 액션 런타임 전체 시험 — 2026-09-13

**전체 시험은 아직 통과가 아니다.** 명시한 273파일의 3,515사례를 끝까지 실행해 **3,507 통과 / 6 실패 / 1 skip / 1 todo**로 집계했다. 이후 웹 프레임 보수를 포함한 관련 9파일은 **158/158 통과**했다. 두 실행에는 중복 사례가 있으므로 수를 합산하지 않는다.

현재 제품 범위는 [액션 실행부 시험 결과](액션런타임_시험결과_20260912.md)의 **개발 전용 액션 시험 기반**이다. 사용자가 거절한 문장 활쏘기·문항팩·학습 연결을 재도입하지 않았다. 이 문서는 Node 합성·구조 시험의 결과이며, Android 조립·설치·실기기 속도·운영 배포·학생 학습 효과의 완료 증거가 아니다.

## 실행 기준과 실제 집계

- 작업본: `C:/Users/q1212/Documents/SYNK-talk`, Node `v24.18.0`.
- 시작·종료 HEAD: `7d3cd56c538ad2c3dcea726f1ddad283f4d5ef5d`. 형제 appsscript HEAD 조회값: `a106aa8a666c19a448130e3602bf3940925ddb5c`.
- 미커밋·미추적 변경이 있는 공유 작업본이다. `session-freshness`의 ahead/behind는 0이나 작업본 지문 상태는 `incomplete`였다. 이를 내용 동일·원격 최신의 증거로 사용하지 않고 현재 파일과 관련 diff를 직접 읽었다.
- 재개 전 중단 세션 `3844`는 종료됐고 잔류 시험 프로세스 0개를 확인했다. 그 중단 로그는 최종 집계에 포함하지 않았다.
- `rg --files tests -g '*.test.js' -g '*.test.mjs' -g '*.test.cjs'`를 정렬해 **273파일을 명시**했다. `tmp/`·빌드 산출물의 자동 시험 수집은 하지 않았다. 새 `tests/액션시험대.test.js`의 8사례가 포함됐다.
- 기존 `tests/lib/띄우기.js`로 Node를 실행했다. 파일 동시 실행 수는 `--test-concurrency=1`; 프로세스 미실행·비정상 종료와 시험 실패를 구별하는 기존 통로를 유지했다.

| 실행 | KST 시작 → 종료 | tests | pass | fail | cancelled | skipped | todo | 시험 소요 |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| 전체 명시 273파일 | 00:39:38 → 00:42:40 | 3,515 | 3,507 | 6 | 0 | 1 | 1 | 181.787초 |
| 웹 보수 후 관련 9파일 | 00:44:22 → 00:44:26 | 158 | 158 | 0 | 0 | 0 | 0 | 3.844초 |

전체 실행은 exit `1`, 보완 실행은 exit `0`으로 정상 종료했다. 전체 실행의 skip은 POSIX `sh`가 없어 커밋 트레일러 훅을 실행하지 못한 1사례다. todo는 기존 과제 생성 평가·사람 채점 게이트의 미실행 1사례다. 어느 것도 통과로 합치지 않았다.

동일한 명시 수집 방식의 재현 명령:

```powershell
$runtimeTestFiles = @(rg --files tests -g '*.test.js' -g '*.test.mjs' -g '*.test.cjs' | Sort-Object)
node --test --test-concurrency=1 @runtimeTestFiles
```

이 명령을 나중에 실행하면 **그 시점의 목록**을 센다. 이번 전체 실행 도중 새 `tests/액션웹프레임.test.js`가 추가되어 종료 후 목록은 274파일이었다. 따라서 273파일 전체 결과를 최종 274파일의 동일 시점 전체 실행으로 바꿔 부르지 않는다.

## 실패 6사례의 분리 — 원인 5갈래

| 분류 | 실패 위치·사례 | 확인한 원인과 근거 | 남은 조치 |
|---|---|---|---|
| 기존 HEAD 파일과 현재 정본의 불일치 | `tests/강사지식.test.js:174` — 생성물이 원본과 같다 | 강사지식 빌더의 `--확인`이 생성물 불일치를 반환했다. 시험·빌더·목록·생성물은 HEAD 대비 변경 없음. 이전 전체 측정에서도 같은 증상이 있었다. | 현재 승인 정본과 생성 경로를 해당 작업에서 대조. 이번에는 생성물을 재작성하지 않음. |
| 기존 HEAD 파일과 현재 정본의 불일치 | `tests/깊이.test.js:126` — 정본 수식 전량 대조 | 몽글 311번 칸 `0.64 ≠ 0.639`. 시험·`contents/깊이.json`·반입 도구는 HEAD 대비 변경 없음. 이전 측정과 같은 값의 불일치. | 자산 담당 범위에서 원천·생성기를 대조. 이번에 값이나 자산을 손으로 보정하지 않음. |
| 기존 HEAD 시험 통로 불일치 | `tests/스폰통로.test.js:172` — 모든 테스트가 공용 통로를 쓴다 | `tests/교수장면자산.test.js:71`의 직접 Node spawn을 탐지. 두 시험 파일 모두 HEAD 대비 변경 없음. | 해당 시험에서 기존 `tests/lib/띄우기.js` 통로 사용을 검토. 보호 단언·스캔 제외를 추가하지 않음. |
| 기존 HEAD 도구 등록 불일치 — 2사례 | `tests/플래그게이트.test.js:155`, `:169` — 게이트 없는 도구 명단·갈래 | `tools/가이드장면자산반입.js`, `tools/가이드장면표정반입.js`가 게이트 없는 도구로 탐지된다. 명단은 비어 있고 각각 위치 인자/플래그 읽기 형태. 두 도구와 시험은 HEAD 대비 변경 없음. | 해당 도구 범위에서 실제 허용 인자와 기존 게이트를 연결. 명단 상한 완화·검사 삭제하지 않음. |
| 이번 최신화의 Git index 통합 전 과도 상태 | `tests/구문검사.test.js:100` — 추적 소스 전량 파싱 | 추적 소스 583개 중 582개를 읽었다. 읽기 전용 대조에서 부재 파일은 삭제한 `tools/스키아패치.js` **정확히 1개**였다. 삭제가 아직 index에 통합되지 않아 `git ls-files`가 파일을 내놓는 상태다. | 정상 삭제를 정확 범위로 Git에 통합한 뒤 원래 검사를 재실행. 파일 복원·검사 제외·분모 완화로 통과시키지 않음. |

분류의 한계: 깨끗한 HEAD 전체를 별도 checkout해 실행한 결과는 아니다. 위의 기존 문제 분리는 **해당 시험·원천 파일의 HEAD 동일성, 실제 오류, 이전 측정과의 일치**에 근거한다. 현재 형제 저장소 정본을 읽는 시험은 그 정본의 상태에도 영향을 받는다.

이번 실행에서 사용자/다른 담당의 dirty 변경이나 액션 런타임 동작에 직접 귀속되는 실패는 관측되지 않았다. 이것을 그 변경 전체의 완전성 증명으로 확대하지 않는다. 기존 dirty `교정배치`, `조용한실패`, `처리장부`, `회차장부` 및 서버/SQL 원본은 수정하지 않았다. 시작부터 미추적이던 다른 작업의 `tests/교정자동화보호.test.js`도 그대로 보존했다. 이 담당이 앞선 통합에서 바꾼 부분은 Postgres.js 대역 이름 2줄(`:95`, `:325`, 현재 `3.4.9`)뿐이다.

## 실행 중 변경과 웹 보완

전체 실행의 시작·종료에 모든 명시 시험 파일과 App·운동 계산·네이티브/웹 어댑터·시험대·패키지 7파일의 지문을 비교했다. 변화는 `src/액션운동장.web.js` **1파일**이었다. 전체가 변하지 않은 단일 스냅샷을 본 것처럼 보고하지 않는다.

별도 검토에서 발견한 웹 측정 누계 잔류 문제를 웹 담당이 수정했고 `tests/액션웹프레임.test.js` 7사례를 추가했다. 보완 시험은 해당 수정 완료 뒤 실행했으며 시작·종료의 관련 지문 변화는 **0파일**이었다.

보완 9파일: `액션실행부`, `액션프레임`, `액션시험대`, `액션웹프레임`, `dependency-upgrade`, `미정의심볼`, `화면구문`, `감사회귀_W2C4`, `소스검사통로`의 `.test.js` 파일. 모두 기존 보호 기준을 유지해 158/158 통과했다.

- 시험대 8사례: 자동 입력 20회 상한, 수락과 입력의 분리, 초기화 전 지연 결과 차단, 닫기/unmount 정리, 백그라운드·수동 중지, 지난 자동 회차 타이머 차단.
- 웹 프레임 7사례: 숨김/수동 중지 전 측정 누계 폐기, 복귀 첫 긴 간격 제외, 초기 숨김, seed 초기화, RAF·구독 정리, 동작 줄이기·최신 콜백 유지.
- 실제 화면 소스의 상태·효과·콜백에 합성 시간을 넣은 시험이다. 브라우저 실측 FPS·Canvas 픽셀·네이티브 스레드 성능을 측정한 시험은 아니다.

## 철회 범위 유지 확인

- App의 시험대 require(`:18`), 화면 분기(`:256`), 진입 버튼(`:275`)는 모두 `__DEV__` 조건 안이다. 현재 화면은 돌아가기 콜백만 받고 학생 식별자·문항·서버 인자를 받지 않는다. 화면의 결과 콜백은 로컬 계수/표시만 갱신한다.
- 시험대·네이티브/웹 어댑터·순수 계산 4파일의 실제 import를 파싱해 UI/운동/테마 의존만 확인했다. 학생 저장·게임 제출·서버 API 통로가 없다. 활성 `lib/src/supabase/tools/C0`에서 제거한 학습 모듈·API·문항팩·사건 식별자 참조는 0건이었다.
- `lib/게임학습.js`, `src/액션학습API.js`, `contents/활쏘기문항.js`, `tests/액션학습.test.js`, `src/활쏘기화면.js` 모두 부재다.
- 기존 events/progress와 progress 시험의 현재 diff는 Postgres.js `3.4.9` 각 1줄뿐이다. 게임 스냅샷·복습 파생 모듈에는 이번 학습 실험 diff가 없다.
- 이 확인은 활성 소스 범위다. 옛 export 폴더나 최종 배포 바이너리가 같은 상태라는 주장은 하지 않는다. 최종 번들·기기 검증은 통합 담당의 별도 결과를 따른다.

## 증거 지문과 남은 범위

전체 시작 시 정렬된 시험 경로와 파일 바이트를 순서대로 합성한 SHA-256: `522f19ddb3cb4aa80b6d8bd21d77c29bc313634ce59ea3f8606aeb391e7b0e41`.

| 보완 완료 후 파일 | SHA-256 |
|---|---|
| `App.js` | `DCEF5D680364A0E3B87F89BEA44070D33A7793419BD7896C580D584A5211363E` |
| `lib/액션실행부.js` | `2E94BE81E4FF0CAF14D9BF58DB7BFFB4C98E2D2912525CBBB57C45407A17BD55` |
| `src/액션운동장.native.js` | `0A0256350533A7F993C17F83892EA4F07354A2D529A58FC5D7412B6C8AB6EC7C` |
| `src/액션운동장.web.js` | `5247CC956979D657D67528BF43A13BFDFACC5EE2FECE6425B034BD7BA41E74CB` |
| `src/액션테스트화면.js` | `E7241C98623846C221A93698B2E510BCDEF00031221AAB46301DE085A1192860` |
| `package.json` | `FE596342EC734E207538C861ACA33063DF3DE92ED0B4BFA6460521CCCA5BFED7` |
| `package-lock.json` | `5494900895AE11E14FE7DBCC3BD821521FE6C25411BE9390124E6EB7A2E5880A` |

로컬 원시 TAP 로그:

- `tmp/action-runtime-full-20260913-resumed.tap` — SHA-256 `6DF9CFD03806A537DDBAF0A2C31C9154D210790FDA8663993A23CA170D9714EC`.
- `tmp/action-runtime-followup-20260913.tap` — SHA-256 `AFE1C84CA4676DF3F6BB7B82E54782E1FB7AC3E59FBB8DA541F5C000F26F031D`.

이 담당의 이번 재개 변경은 이 결과 문서뿐이다. 시험 코드·생성물·검사 기준을 수정하지 않았고 커밋·push·배포·운영 DB 쓰기를 하지 않았다. 남은 일은 위 기존 불일치의 별도 처리, 정상 삭제의 Git 통합 후 원래 구문 검사 확인, 통합 담당의 최신 번들/실기기 검증이다. 범위 밖의 학습 실험이나 학생 통로를 다시 만드는 것은 남은 일에 포함하지 않는다.
