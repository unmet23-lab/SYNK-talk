# Talk AI 품질 결함 수리 · 2026-09-11

사용자 요청: 9월 11일 현재 받아쓰기·교정을 성능 우선으로 재검토하고, 확실히 좋은 대안을 추천한다. 실제 결함은 수리하되 모델명만 바꿔 품질이 좋아졌다고 간주하지 않는다.

## 변경

- `22d9804`: lib/전사.js·lib/검수확정.js의 JSON 구간 확률에서 null·빈 문자열·boolean 등 암묵 숫자 변환 제거. 실제 숫자 0은 보존.
- `d7fab09`: lib/교정엔진.js는 오류없음+실제 오류 태그의 상충 응답을 거부. tools/eval-score.js는 미검출과 통과 동시 판정 방지.
- `30736cf`: 검수 화면에서 미측정 scalar 신뢰도를 null로 표시하지 않음. DB 십진 숫자 문자열은 자릿수를 유지. JSONB 구간 계약과 구분.
- 기존 모델 Whisper-1·Sonnet 5, 오디오 원본·사람 확정 전사·최소 청취 게이트·DB 계약 유지. 과거 잘못 저장된 값은 소급 수정하지 않음.

## 검증

통합 관련 19파일 353/353 통과. 최종 UI 통합 후 서버 배포 입력 77파일은 통합 시험판 d7fab09와 바이트 동일. STT·교정·UI 패치의 독립 검토에서 새 P0/P1 회귀 없음. 기존 20문항 평가 보관 출력 4벌은 여전히 각각 18/20으로 점수 변경 없음.

리허설 배포 SHA: `30736cfac78d196ccd21e6094c69493975188ed2`.

| 함수 | 버전 | 동봉 원문 | 본체 앵커 |
|---|---:|---:|---:|
| events | 81 | 10/10 | 161/161 |
| transcribe | 36 | 4/4 | 74/74 |
| review | 39 | 8/8 | 200/200 |
| teach | 52 | 20/20 | 390/390 |
| correct | 64 | 12/12 | 164/164 |
| companion | 46 | 9/9 | 88/88 |
| deliver | 105 | 42/42 | 522/522 |
| deliver-one | 50 | 35/35 | 111/111 |

전부 ACTIVE, 기존 배포대조 8/8 ‘같다’. 동봉 140개는 전체 원문을, 변환되는 TypeScript 본체는 기존 도구의 주석 앵커·배포 시각을 대조한 결과다. 본체 전체 바이트 일치라고 보고하지 않는다.

배포장부 기존 615행/95,494바이트의 SHA-256 `fa359b94903cf69b7cb11c090471f299b8c629b825b66963b3d78d23f665d197` 보존, 자기 배포 8행(616~623)만 추가. 다른 작업본·스테이징에는 쓰지 않음.

## 남은 상태

- Anthropic 잔액 부족으로 실제 모델 단건 성공·새 모델 비교는 미완료. 이번 후속의 유료 모델 호출·키 변경·결제는 0건.
- 운영 배포는 하지 않았다. 검수 화면 수정은 코드·로컬 렌더 검사이며 앱 실배포·실기기 픽셀 검증은 별도다.
- 받아쓰기 gpt-transcribe와 Gemini 3.5 Transcribe verbatim은 비교 후보다. 현행 구간 시간·신뢰도·학습자 오류 보존을 대체할 실측이 없으므로 Whisper 유지.
- 교정은 Sonnet 5 유지, Opus 5와 동일 단건 요청·문맥·몽골어 의미를 비교하는 평가를 우선 권한다. 현행 18/20×4 Gemini 구독 묶음 선별은 제품 API 모델 간 대조 성적이 아니다.
- 전체 시험은 이번 좁은 수정에서 재실행하지 않았다. 직전 전체 시험의 기존 원본/반입본 관련 3실패를 이번 관련 353개 성공으로 덮지 않는다.
