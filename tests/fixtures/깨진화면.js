'use strict';
/* 픽스처 — **F268 그 모양**(어디에도 없는 이름을 화면이 부른다).
 *
 * 실제 사고: `src/말하기화면.js` 가 `useState(오늘)` 을 불렀는데 `오늘` 은 선언이 없었고,
 * 그때 저장소는 550/550 초록이었다. 구문검사는 문법만 보고 나머지 테스트는 순수 함수만
 * 불러서 **화면을 한 번도 안 그렸기** 때문이다. 이 파일은 그 형태를 고정해 둔 것이고,
 * `tests/화면렌더.test.js` 가 「이 하네스는 그것을 실제로 잡는다」를 여기서 못박는다.
 *
 * 🔴 고치지 않는다 — 깨져 있는 것이 이 파일의 일이다.
 * 🚫 `src/`·`lib/`·`contents/` 로 옮기지 않는다: `tests/미정의심볼.test.js` 가 그 셋을 훑어
 *    정적으로 잡으므로, 거기 두면 이 픽스처가 그 검사를 빨갛게 만든다(자기 처방 충돌).
 */
import { Text, View } from 'react-native';

export default function 깨진화면() {
  // `없는이름` 은 이 파일 어디에도 없다 — 번들링·구문검사는 통과하고 **그릴 때** 죽는다.
  return (
    <View>
      <Text>{없는이름.값}</Text>
    </View>
  );
}
