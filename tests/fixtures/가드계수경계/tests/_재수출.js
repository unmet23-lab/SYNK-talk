'use strict';
/* 픽스처 — 공용 통로를 «별칭으로» 들여와 다시 내보낸다(as `tools/실행층점검.js:57` 의 그 모양).
 * 돌아가지 않는다(파싱 재료다 · `.test.js` 를 안 붙인 이유).
 * 계수기의 `내보내는생산자` 가 이 별칭을 «정제»로 알아보지 못하면, 이것을 받은 소비자가
 * 통로 그 자체를 위험으로 센다 — 같은 병 5차(「이 파일만 봤다」의 남은 반쪽). */
const { 줄맞춰코드만: 지우개 } = require('../../../lib/소스검사.js');

module.exports = { 지우개 };
