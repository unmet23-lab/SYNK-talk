/* 학생에게 나가는 «앱이 짓는 글»의 금지 패턴 — 정본 한 곳.
 *
 * 처음 산 곳은 tests/꼬리.test.js(꼬리 문구 회귀)였는데, 캐릭터 혼잣말 회귀
 * (tests/마스코트생명.test.js)가 **같은 정규식**을 재사용하게 되면서 여기로 빼 왔다
 * (생명감 설계 §3: 「tests/꼬리.test.js 의 금지패턴과 같은 정규식을 재사용한다 —
 *  두 벌로 적으면 갈라진다」). **탐지력(가짜 문구가 실제로 걸리는지)은 tests/꼬리.test.js 가
 * 계속 진다** — 여기는 목록만 산다(검사 로직·픽스처를 여기로 끌고 오지 않는다).
 *
 * 🔑 고유어 수사를 빠뜨리면 이 가드는 정작 필요한 자리에서 샌다 — 철학 본문의 예시가
 *   하필 「지난주엔 **세 번** 틀렸는데」라, 한자어 수사만 세던 첫 판은 그 문장을 통과시켰다
 *   (2026-08-12 실측 · 꼬리 탐지력 픽스처가 잡았다).
 * 🔑 붙여쓴 낱말은 안 센다 — `이번`·`한글` 이 수사로 읽히면 실문구가 통째로 빨개지고, 그렇게
 *   시끄러운 가드는 곧 꺼진다. 그래서 한글 수사는 **띄어쓰기를 요구**하고 아라비아 숫자만
 *   붙여쓰기를 본다.
 */
'use strict';

const 한글수사 = '한|두|세|네|다섯|여섯|일곱|여덟|아홉|열|스무|일|이|삼|사|오|육|칠|팔|구|십|백|천|만';
const 단위 = '번|개|회|점|등|군데|가지|자리|줄|문장';

const 금지패턴 = Object.freeze([
  { 이름: '숫자', 정규식: new RegExp(`[0-9]|(?:${한글수사})\\s+(?:${단위})`) },
  { 이름: '남과의 비교', 정규식: /친구|다른 (학생|아이|사람)|평균|등수|순위|반에서/ },
  { 이름: '점수·등급', 정규식: /점수|퍼센트|%|등급|레벨|랭킹/ },
  /* 「잘했어요」류 — 대조지 채점이 아니다(`lib/꼬리.js` 문구 절). 변이 M6(평가어 주입)이
     이 목록에 평가어 축이 없어 살아남았다(2026-08-13 적대 리뷰 실측). */
  { 이름: '평가어', 정규식: /잘했|잘 했|훌륭|최고|대단|멋져|멋있|완벽|천재/ },
]);

module.exports = { 금지패턴 };
