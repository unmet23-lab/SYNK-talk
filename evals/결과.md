# 교정 엔진 평가

프롬프트 `prompts/교정.md` · 픽스처 `evals/픽스처.json`(**합성 102문항** — 오류 62 = 단일 46 + 복합 16 · 정상 40)

재현: `node tools/eval-run.js --원격 --출력 evals/출력_<판>.json` → `node tools/eval-score.js evals/출력_<판>.json`

---

## 🔴 2026-08-12 — 첫 «진짜» 기준선이 섰다. 그리고 그 첫 측정이 두 개의 거짓을 드러냈다

08-03·08-08 의 수치는 전부 **대화 세션이 손으로 쓴 출력**이었다(이 파일이 스스로 「실제 엔진
경로가 아니다」라고 적어 둔 그것). 08-12 에 `tools/eval-run.js --원격` 이 서면서 같은 102문항이
**리허설 `correct` 를 통해 벤더로** 나갔다 — 채점한 것과 제품이 하는 것이 처음으로 같아졌다.

그 첫 측정에서 나온 숫자가 **두 번 틀렸고, 둘 다 우리 쪽 결함이었다.**

### ① 채점기가 「답이 없는 것」을 「과교정」으로 세고 있었다

첫 판독은 `거짓양성 6/40` 이었다. 실제로 과교정한 것은 **2건**이고 나머지 4는 응답을 아예 못
읽어 버린 항목이다. 이 파일이 08-08 에 「막았다」고 적어 둔 바로 그 오독이 **다른 문으로 다시
샜다** — 그때의 자물쇠는 「그 id 가 출력 파일에 **없다**」만 잡는데, `eval-run` 은 버린 응답도
행으로 남긴다(`{id, 검증사유:'형식밖'}`). null 이 아니라 자물쇠를 그대로 통과했고, `고친문장` 이
없으니 빈 문자열로 읽혀 「맞는 문장을 고쳤다」가 됐다.

처방 = **무응답**을 세 축(거짓양성·교정·태그)에서 갈라 낸다. 단 **판정불가로 빼지 않는다** —
제품에서도 그 응답으로는 교정행이 안 서니 진짜 실패다. 전체통과 분모에는 실패로 남기고 세 축의
분모에서만 뺀 뒤, 줄어든 분모를 요약이 스스로 말한다(「줄어든 분모는 좋아진 것이 아니라 답이
없던 것이다」). 가르는 술어는 사유 문자열이 아니라 **채점할 답이 있는가**다 — 빈 문자열은
모델이 실제로 낸 답이라 그대로 실패로 센다.

### ② 잃던 것은 모델 품질이 아니라 «파서»였다 — 교정의 최대 손실원

무응답 8건(7.8%)의 원인을 실측했다. **토큰 고갈로 잘린 것이 아니다.** 모델이 프롬프트의
「JSON만, 다른 말 금지」를 받고도 머리말·꼬리 해설 사이에 ```json 울타리를 끼워 답하는 일이 있고,
옛 `울타리벗기기` 는 울타리가 글 **전체**일 때만 벗겼다. 그러면 글 전체가 파싱에 들어가 통째로
버려진다.

🔑 **꺼내 보니 내용은 정확한 답이었다.**

```
E30  "선생님, 이거 뭐예요?"                    + 높임:상대(존댓말 등급)     ← 픽스처 기대와 일치
M01  "어제 학교에 갔어요. 그리고 친구를 만났어요."  + 조사:처소 · 어미:시제      ← 복합 오류 둘 다 잡았다
```

학생 쪽에서 보면 이건 「첨삭이 틀렸다」가 아니라 **「그날 첨삭이 그냥 안 온다」**다. 처방 =
`응답덩이꺼내기()`(①울타리 안 → ②글 전체 → ③산문에 박힌 중괄호 덩이 순 · 그중 `고친문장` 을 든
덩이 우선). 느슨해진 만큼은 `교정값()` 이 그대로 진다 — 꺼내기는 판정이 아니다.

⚠ **프롬프트 문구만으로는 0 이 안 된다** — 비결정적이다(같은 8건을 다시 돌리자 6건은 손대지
않았는데 그냥 통과했다). v2 가 「JSON 만」을 다시 못박은 것은 빈도를 줄이는 몫이고, 막는 자리는
파서다.

⚠ **못 읽은 응답의 원문을 이제 남긴다** — 안 남기면 `형식밖` 이 진단 불가다. 잘린 JSON(예산
재산정)과 산문 동봉(프롬프트 문구)은 처방이 정반대인데 파일엔 사유 한 낱말만 남아 둘이 같은
모양이었다. 위 8건 중 **원인이 실측된 것은 원문이 보존된 2건뿐**이고, 나머지 6건의 첫 회차
실패 원인은 **모른다** — 지어내지 않는다.

### v1 실측 (2026-08-12 · claude-sonnet-5 · thinking off · 리허설 correct)

```
                    첫 판독      채점 정직화 후   + 파서 수리
거짓양성            6/40         2/36            2/40
교정 정확          46/62 74%    46/58 79%       49/62 79%
태그 정확          47/62 76%    47/58 76%       51/62 82%
전체 통과          73/102       73/102          80/102
무응답             (안 셌다)     8               0
```

`출력_v1api.json` = 첫 실행 원본(건드리지 않는다) · `출력_v1api_진단.json` = 파서 수리 효과를
재는 파일(회차가 섞여 있다 — 94건 첫 실행분 + 8건 재실행분, 그중 2건은 보존된 원문 재판독).

**손기준 100% 와의 낙차는 이 파일이 예고한 방향 그대로다.** 08-03 의 `21/21 100%` 는 픽스처를
만든 주체가 답도 썼기 때문이고, API 는 정답을 모른다. 그 낙차가 **처음으로 측정된 것**이 이번
기준선의 값어치다.

거짓양성 2건은 둘 다 진짜다:
- `C10` 「빨리빨리 오세요」→「빨리 오세요」 — 규칙 3 위반(틀리지 않았는데 자연스럽게 바꿨다).
- `C11` 「김치가 제일 좋아요」→「김치**를** 제일 좋아요」 — **맞는 문장을 틀리게 만들었다.**
  「좋아하다」로 읽고 조사를 바꿨는데 원문은 「좋다」로 옳다. 가장 나쁜 실패 모양이다.

---

## v2 (2026-08-12) — 오늘의포인트 몽골어 + 학생 맥락 절

바뀐 것은 `prompts/교정.md` 버전 이력 v2 줄에 있다. 재는 것은 둘이다.

1. **품질이 안 내려갔는가** — 같은 102문항, 맥락 없이. v1 과 직접 대조되는 자리다
   (맥락이 없으면 요청이 v1 과 **바이트 동일**이도록 배선했고 회귀가 그것을 문다).
2. **맥락이 판정을 오염시키지 않는가** — `evals/맥락표본.json` 12건을 맥락 유/무로 돌려
   `고친문장`·`오류태그`가 같아야 한다. 개인화 대상은 `오늘의포인트` 한 칸뿐이다.

### 실측 (2026-08-12 · correct v14)

```
                    v1 + 파서수리    v2
거짓양성            2/40            2/40
교정 정확          49/62 79%       49/62 79%
태그 정확          51/62 82%       48/62 77%
전체 통과          80/102          79/102
무응답             0               0
오늘의포인트 몽골어  —               102/102   ← 키릴 문자 기준. 한국어 표기는 문장 «안에» 인용됐다
```

**②맥락 오염 — 신호 없음.** 같은 12문항을 세 번 돌렸다: A(맥락 없음) · B(맥락 있음) ·
**C(맥락 없음, 두 번째)**.

```
맥락 유/무 다름              3/12   (A↔B)
같은 입력 두 회차 다름       4/12   (A↔C = 잡음 바닥)
```

맥락으로 인한 다름이 **잡음 바닥을 넘지 않는다.** 그리고 개인화는 설계한 자리에만 나타났다 —
`C02` 는 맥락 없이는 「`을/를`·`-면서` 를 바르게 썼다」로 끝나는데, 맥락이 붙으면
「**최근 자주 틀리던 그 자리인데 이번엔 맞았다**」로 바뀐다. 판정(`고친문장`·`오류태그`)은 그대로다.

---

## 🔴 그런데 이 표에서 가장 중요한 숫자는 **4/12** 다

**같은 입력을 두 번 돌리면 12문항 중 4개가 다르게 온다(33%).** 프롬프트도, 요청 바이트도,
모델도 같다. 이건 관찰이 아니라 **측정된 잡음 바닥**이고, 여기서 두 가지가 따라 나온다.

1. **위 v1↔v2 비교는 판정을 못 낸다.** 80 vs 79 는 5문항이 갈린 결과이고(나빠짐 3·좋아짐 2)
   그 폭은 잡음 바닥 안이다. 「v2 가 태그에서 나빠졌다」고 읽으면 안 되고, 「좋아졌다」고 읽어도
   안 된다. 🔑 **한 판에 한 회차씩 돌려 견주는 방식 자체가 이 분산에서 성립하지 않는다.**
   이 파일이 「프롬프트를 고칠 때마다 이전과 비교할 수 있다」고 적어 온 것은, 회차를 늘리기
   전까지는 **아직 참이 아니다.**
2. **원인 후보가 하나 뚜렷하다 — `temperature` 를 안 준다.** `요청몸통()` 은 `model`·
   `max_tokens`·`thinking`·`system`·`messages` 만 싣고 온도를 안 싣는다. 벤더 기본값은 1.0 이다.
   교정은 창작이 아니라 판정에 가까우므로 낮출 이유가 분명하고, 낮추면 이 분산의 상당 부분이
   사라질 것으로 **보인다 — 아직 안 쟀다.**

⚠ **온도를 이번에 안 건드렸다.** 바꾸면 위 표 전체가 다른 온도에서 잰 값이 되어 기준선이
못 쓰게 된다. 순서는 ①온도를 정하고 ②잡음 바닥을 다시 재고 ③그 위에서 v1↔v2 를 다시 견주는
것이다 — 재지 않고 바꾸면 「나아졌다」를 증명할 근거가 사라진다.

## 관찰 — v2 산출물에서 눈에 띈 것 2

- `C13` 「내일 시험이 있어서 오늘 열심히 공부해요」→「공부**할 거예요**」. 새 거짓양성인데,
  v1 에서 거짓양성이던 `C10` 은 반대로 고쳐졌다 — 개수는 2/40 로 같고 **자리만 바뀌었다**
  (이것도 잡음 바닥 안이라 v2 탓으로 못 돌린다).
- 몽골어 문장 안에 한국어 문법 «용어»가 붙어 오는 자리가 있다(`гэж목적격 тусгай`). 학생이
  찾아야 하는 글자(`을/를`)는 인용이 맞지만 **용어**(`목적격`)는 몽골어여야 한다 — 프롬프트
  한 줄로 좁힐 자리이고, 몽골어 검수와 함께 처리한다.

---

## 🔴 이 수치가 말하지 않는 것 — 먼저 읽을 것

세 한계 중 **하나가 08-12 에 사라졌다**(실제 엔진 경로). 둘은 그대로다.

1. ~~실제 엔진 경로가 아니다~~ → **해소.** `eval-run --원격` 이 리허설 `correct` 를 지나
   벤더로 나간다. 요청 조립·응답 해석 모두 `lib/교정엔진.js` — 제품과 **같은 코드**다.
   ⚠ 그 대가로 **배포판이 낡으면 측정이 무효**라, 실행기가 응답 `prompt_ver` 를 로컬 파일 판과
   대조해 다르면 실행 자체를 거부한다(이중 자물쇠).
2. **오류 분포를 모른다.** 유형별로 고르게 만들었지만 실제 몽골 학생은 특정 유형에서 압도적으로
   많이 틀린다. 어디에 힘을 쏟을지는 실학생 문장으로만 알 수 있다.
3. **거짓양성은 「내가 예상한 함정을 피했다」는 뜻**이다(정상 40개). 진짜 위험은 설계자가
   예상하지 못한 함정이고, 그건 실학생 문장으로만 나온다.

## 그래서 실제로 증명된 것

- **하네스가 돈다** — 픽스처 → 벤더 → 기계 채점 → 종료코드. 프롬프트를 고칠 때마다, **그리고
  새 모델이 나올 때마다** 같은 102문항으로 이전과 비교할 수 있다(제품방향 불변식 5).
- **태그 어휘가 수집층과 붙는다** — 23종을 SYNK-appsscript `HW_ERROR_TAGS` 에서 그대로 가져왔고,
  이제 23종을 전부 시험한다. 갈라지면 2년치 집계가 깨지므로 `태그어긋남()` 이 돌기 전에 대조한다.
- **채점을 세 축으로 나눈 것이 값을 했다** — 합산 하나였다면 거짓양성이 탐지율에 묻힌다.
  제품에서 더 나쁜 실패는 「맞게 쓴 학생 문장을 고치는 것」이다(`C11` 이 그 실물이다).
- **그리고 이번엔 축을 «네 개»로 갈라야 했다** — 「답이 안 왔다」가 「과교정했다」와 같은 칸에
  들어가 있던 것이 첫 판독을 뒤집었다.

## 관찰 1건 — 최소 수정과 교육적 교정이 부딪힌다 (2026-08-03 · 유효)

`E12` 「우리 선생님은 한국어를 잘 배워요」에서 최소 수정이면 `가르쳐요` 지만 실제 교사는
`가르치세요` 로 고친다(주체 높임). **어느 쪽이 옳은지는 교육 방침의 문제이지 엔진의 문제가
아니다.** ⚠ API 기준선에서 이 문항은 `가르쳐요` 로 왔고 태그 `높임:주체` 를 못 붙여 실패로
찍힌다 — 픽스처가 교사 쪽 답을 기대값으로 들고 있어서다. 이 갈림을 닫으려면 프롬프트에
「선생님이 주어면 주체 높임까지」를 명시하거나 픽스처 기대를 낮춰야 하고, **둘 다 교육 방침
판정이라 엔진이 혼자 정하지 않는다.**

## 다음에 할 것 (순서대로)

0. ~~새 76문항을 실제로 돌린다~~ → **✅ 08-12 종결**(102/102 · 판정불가 0).
1. 🔴 **`temperature` 를 정하고 잡음 바닥을 다시 잰다.** 이게 1번인 이유: 잡음 바닥이 33%면
   그 아래 모든 비교가 판정 불능이고, **판정 불능인 하네스 위에서 프롬프트를 고치는 것은
   기준선 없이 고치는 것과 같다**(이 파일이 08-03 에 「기준선 없이 프롬프트를 고치면 나아졌는지
   알 수 없다」고 적은 그 자리다). 순서 = 온도 확정 → 같은 12건 2회차로 바닥 재측정 →
   그 위에서 v1↔v2 재대조.
2. **실학생 문장 20개** — 유호님 또는 강사. 이걸 넣는 순간 위 한계 2·3 이 사라진다.
   (합성 픽스처는 덮지 않는다 — 「유형 커버리지」와 「우리 학생의 분포」는 다른 질문이라
   `--fixture` 로 갈라 채점한다.)
3. **독립 채점** — 픽스처 정답을 보지 않은 채점자(다른 세션 또는 사람)로 한 번.
4. **몽골어 해설 검수** — v2 의 `오늘의포인트` 는 몽골어다. 품질 확정은 검수자 몫이고,
   AI 행은 1차안이라 되돌릴 수 있다(선행 = 검수자 1인 · 결정큐 기존 항목). 문법 «용어»가
   한국어로 새는 자리(`гэж목적격`)를 같이 좁힌다.
5. `C11` 유형(맞는 문장을 **틀리게** 만드는 과교정)이 잡음 바닥 위에서도 남으면 규칙 1 을 손댄다.
