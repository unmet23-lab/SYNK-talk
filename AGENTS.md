# SYNK-talk — Codex 작업 지침

작업 시작·재개 때 다음 원문을 실제로 읽는다. 링크가 보였다는 사실은 대상 전문을 읽었다는 뜻이 아니다.

1. [공통 원칙 연결](docs/공용지침.md)이 가리키는 SYNK-appsscript `docs/AI_운영원칙.md`와 Codex `AGENTS.md`.
2. [이 저장소 규약](docs/이_저장소_규약.md)과 이번 기능에 필요한 제품·데이터·API 계약.
3. 공통 원칙의 「세션 사이 최신 상태」에 따라 현재 talk 작업 사본과 appsscript 기준 원문의 변경을 확인한다. 명령과 경로는 [공통 원칙 연결](docs/공용지침.md)에 있다.

직접 받은 작업은 조사·제작·검증·통합·보고까지 책임진다. 고정된 모델 간 발주 순서나 과거 역할표로 현재 요청의 권한을 줄이지 않는다. 명시적으로 `tools/codex-build.js`의 기술 실행자로 호출된 경우에만 그 실행 계약을 따른다.

자격증명·학생 식별 데이터는 출력·저장소·다른 모델 인계에 넣지 않는다. 원본 음성과 동의·철회 계약을 보존한다. 편집 전 `git status`와 관련 diff를 읽고 자기 경로만 커밋한다. 관련 시험은 `node --test tests/<관련 파일>`, 전체 시험은 `npm test`다.
