'use strict';
/**
 * 테마 — 브랜드 정본의 기계 번역.
 * 원천 = SYNK-appsscript docs/디자인_토큰.json 「시맨틱 · 다크」 축 · docs/브랜드_폰트_정본.md
 *
 * 이 앱은 **다크 단일**이다. 그 다크의 이름이 「양모 밤」(유호님 확정 2026-08-20 · 조항 ⓚ)이다.
 *   무채(Graphite/Chalk 12색)를 떠나 양모로 옮겼다 — 낮의 글자(Ink)가 밤의 «면»이 되고,
 *   낮의 바탕(Paper)이 밤의 «글자»가 된다. 새로 난 색은 바탕 `Ink Deep` 하나뿐이다.
 *
 * R1 2색 원칙: 한 화면 = 바탕 1 + 잉크 1 + 신호 1점(면적 5% 미만).
 *   → 이 앱에서 신호 1점 = **녹음 버튼**이다. 코랄은 다른 어디에도 쓰지 않는다.
 * R2 위계는 밀도로: 크기·색 남발 대신 여백과 알파 층으로.
 * R3 타이포 불가침: 글자에 그림자·왜곡 금지.
 *
 * 알파 하한(정본 v1.4.1 · 유호님 확정): 태그 ≥75% · 서브 ≥72% · 메타 ≥62%.
 */

export const 색 = {
  /**
   * 바탕 — **Ink Deep**(양모 밤의 바닥 · 유호님 확정 2026-08-20 · 옛 값 Navy 2 `#0F1730`).
   * Ink 의 그늘이자 옛 Graphite 의 «따뜻한 쌍둥이»다 — 근-검정이되 갈색이 밴 근-검정.
   */
  바탕: '#080605', // Ink Deep
  /**
   * 카드 한 층 위 — **Ink**(낮의 글자가 밤의 면이 된다).
   *
   * 🔑 눈금은 대비비가 아니라 **ΔL\*** 다. 이 트랙이 두 번 걸린 자리라 여기 못박는다:
   *   ① 옛 Navy Ink 는 바탕과 ΔL\* **1.48** 뿐이라 카드가 사실상 안 떠 있었다(08-14 기각).
   *   ② 양모 밤 첫 설계안은 ΔL\* **8.6** 이었는데, 대비비로는 「1.21 통과」로 보였다.
   *      옛 무채 카드 Graphite 2 = **8.26** 이 바로 그 기각된 값이라 같은 실패를 재현할 뻔했다.
   *   바탕을 근-검정까지 내려 **12.77** 을 세웠다(확정된 Graphite 3 = 12.7 과 같은 눈금).
   * 🔴 여기서 더 띄우려고 **Deep Wool `#575046`** 로 올리면 두 색이 그 면에서 죽는다 —
   *   `잉크_보조`(Stone) 4.36 · `신호`(Coral) 2.71. 둘 다 본문 기준 4.5 미달이다.
   *   회귀 = `tests/테마면.test.js` 가 이 면을 픽스처로 물고 있다.
   */
  바탕띄움: '#2B2320', // Ink — 카드 한 층 위
  잉크: '#FBF7F0', // Paper — 바탕 위 18.93 · 카드 위 14.42
  신호: '#F96859', // Coral — 녹음 버튼 전용(바탕 위 6.89 · 카드 위 5.24)
  신호_보조: '#FD9C87', // Coral 2 — 신호의 여린 단(아이콘·테두리)
  눌림: '#AE322A', // Coral 3 — 눌린 상태의 «면» 전용. 🚫 글자 금지(바탕 위 3.17) · 소비 0 — 코랄 면 버튼 소멸로 유물. 🚫 새 소비 금지(눌린 상태는 아래 `눌림감`이 정본)
  실땀: '#F0E3C8', // Stitch — 테두리·구분선·자수 포인트(펠트 오브젝트의 밑단)
  // 알파 층 — 정본 하한을 rgba 로 고정(Paper 251,247,240)
  잉크_태그: 'rgba(251,247,240,0.75)', // 바탕 위 10.56 · 카드 위 8.70
  잉크_서브: 'rgba(251,247,240,0.72)', // 바탕 위 9.78 · 카드 위 8.16
  잉크_메타: 'rgba(251,247,240,0.62)', // 바탕 위 7.33 · 카드 위 6.40
  잉크_희미: 'rgba(251,247,240,0.28)', // 선·비활성 도형 전용(글자 금지 — 하한 미달)
  /**
   * 보조 잉크 2 (**Stone** · 양모 회색 2) — **3번째 글자 층** 전용.
   * 「위계는 밀도」는 1·2층의 규칙이고 3층에선 더 내려갈 자리가 없어 위계가 무너진다.
   *
   * 🔑 정본 다크 축의 `보조잉크`(**Oat** `#EDE7DC`)는 여기 안 옮겼다 — 글자로 쓰면
   *   바탕 위 16.43 으로 `잉크`(18.93)와 1.15배 차이뿐이라 **눈에 안 보이는 층**이 된다.
   *   1·2층은 이 앱의 알파 사다리(10.56 → 9.78 → 7.33)가 이미 맡고 있다. Oat 가 필요해지는
   *   자리는 글자가 아니라 「칩 바닥」인데, 아직 그 화면이 없어서 소비처가 생길 때 연다.
   */
  잉크_보조: '#C7BFB2', // Stone — 바탕 위 11.10 · 카드 위 8.45

  /**
   * 2027 실 넷 — **역할 색**(유호님 확정 2026-08-20 · 조항 ⓚ).
   * 🔑 다크에 «새 실»은 0개다. 라이트의 램프를 **반대편에서** 읽는다 —
   *   라이트는 면=몸통·글자=Deep, 다크는 **면=Deep · 글자=Soft**. 같은 램프가 낮과 밤을 산다.
   * ⚠ 이 여덟은 R1 의 「신호 1점」이 아니라 «역할 표식»이다 — 코랄 자리를 뺏지 않는다.
   */
  안내_면: '#24448C', // Lapis Deep — 안내·힌트
  안내_글자: '#C5D6F5', // Lapis Soft (면 위 6.28)
  정답_면: '#3F6B2E', // Meadow Deep — 정답·통과
  정답_글자: '#D3E8BE', // Meadow Soft (면 위 4.79)
  성장: '#D3E8BE', // Meadow Soft — 면 없이 쓰는 성장 표시(바탕 위 15.46)
  보상_면: '#7E5A10', // Butter Deep — 보상·획득
  보상_글자: '#FFEBB0', // Butter Soft (면 위 5.29)
  기념_면: '#99295D', // Pop Deep — 기념·축하
  기념_글자: '#F8C7DE', // Pop Soft (면 위 5.02)
};

/**
 * 🔑 **R1 「신호 1점」이 화면마다 어디인지 못박는다.**
 *   말하기 화면 = **녹음 버튼**(코랄 면).
 *   인증 화면  = **오류 메시지**(코랄 글자 — 다크 바탕에서만 글자로 쓸 수 있다).
 *   같은 화면에 둘을 함께 두지 않는다 — 인증 화면에는 녹음이 없어서 성립한다.
 * 🚫 인증 화면의 **버튼을 코랄로 칠하지 않는다.** 기본 버튼은 Paper 면 + Ink Deep 글자(대비 18.93)다
 *   — 코랄 면 위 글자는 **Ink(5.24)·Ink Deep(6.89)** 만 허용이라 「색 버튼 + 흰 글자」라는
 *   세상의 기본값이 여기선 미달이다(Paper on Coral = **2.75**).
 */
export const 신호자리 = { 말하기: '녹음버튼', 인증: '오류메시지', 답장: '오류태그', 도착확인: '나가기', 교수멘탈: '보내기버튼', 나침반: '적기버튼', 보고서교정: '짚은어절', 반피드백: '남기기버튼', 알바변명: '녹음버튼', 서류관문: '판정오류표시', 회고: '확정버튼' };

/** 폰트 패밀리 키 — App.js 의 useFonts 등록명과 일치해야 한다. */
export const 폰트 = {
  본문: 'SUIT-Medium', // 500
  강조: 'SUIT-SemiBold', // 600
  헤드: 'SUIT-ExtraBold', // 800
  캡션: 'SUIT-Regular', // 400
  모노: 'DMMono-Medium', // 라벨·타이머 — 로마자·숫자·기호 전용(키릴·한글 글리프 없음)
};

/** DM Mono 라벨 트래킹 — 정본: 짧은 대문자 라벨일수록 크게 벌린다(+0.02~0.24em). */
export const 모노트래킹 = { 라벨: 2.4, 타이머: 0.6 };

/**
 * 몽골어(키릴) 글자 — **킷 한글 폰트에는 이 문자가 없다.** 그래서 자리를 여기 하나로 둔다.
 *
 * ■ ✅ 2026-08-27 — **Inter Tight 를 실었다.** 이 상수가 그날을 위해 비워 뒀던 그 한 줄이다.
 *   실측: 네 굵기 모두 **몽골 키릴 70/70**(러시아어 33자 + 몽골 고유 `Өө`·`Үү`) — `fontTools`
 *   로 cmap 을 직접 셌다(이름만 보고 「키릴판이겠지」로 판정하지 않았다).
 *   실은 것은 셋(Regular·Medium·SemiBold). 🚫 Bold(700)·800 은 안 싣는다 — 몽골어가 헤드로
 *   가는 자리가 지금 없고, 안 쓰는 폰트는 번들만 무겁게 한다. 필요해지는 날 `아래 표`에 더한다.
 *
 * ■ 🔴 SUIT 을 지정하면 **두부(□□□)** 가 뜨고, 그 화면은 「글자가 안 온 것」과 구별이 안 된다.
 *   그래서 몽골어를 그리는 자리는 **반드시** 이 상수(또는 `몽골어폰트`)를 지난다.
 *   ⚠ 그 규칙이 실제로 새고 있었다 — `막힘카드`·`생성카드` 의 «병기» 줄이 `폰트.강조`·`폰트.캡션`
 *     을 박고 있었다(08-27 실측). 증상이 없었던 이유는 그 자리에 아직 몽골어가 안 왔기 때문이고,
 *     오는 날 그 두 화면만 두부가 됐을 것이다. 같은 날 고쳤고 회귀가 그 자리를 문다.
 *
 * 쓰는 법 — 색은 화면이 정한다: `해설: { ...몽골어, color: 색.잉크_서브 }`
 */
export const 몽골어 = { fontFamily: 'InterTight-Regular', fontSize: 15, lineHeight: 25 };

/**
 * 몽골어를 **다른 굵기**로 그려야 할 때. 키 이름은 위 `폰트` 와 일부러 같다 — 화면이 한글 자리에서
 * `폰트.강조` 를 쓰듯 몽골어 자리에서 `몽골어폰트.강조` 를 쓰면, 짝이 눈으로 맞는다.
 *
 * 🚫 `헤드`(800)·`모노` 는 **일부러 없다** — Inter Tight 에 800 이 없고, DM Mono 에는 키릴이 없다.
 *   여기 없는 키를 부르면 `undefined` 가 되어 시스템 폰트로 떨어지는데, 그건 두부보다는 낫지만
 *   브랜드 밖 글자다. 필요해지면 **폰트를 먼저 싣고** 여기 더한다(순서를 뒤집지 않는다).
 * ⚠ `tests/몽골어폰트.test.js` 가 ①여기 값이 App.js 에 실제로 등록됐나 ②그 파일이 있나 를 센다.
 *   빠뜨리면 안드로이드는 그 자리에서 죽고 웹은 조용히 폴백한다 — 후자가 더 나쁘다.
 */
export const 몽골어폰트 = {
  캡션: 'InterTight-Regular',
  본문: 'InterTight-Medium',
  강조: 'InterTight-SemiBold',
};

/**
 * 판 눈금 — 카드 «판»의 반경·안 여백. **값은 현행 실측 그대로다(한 값도 안 바꿨다).**
 *   열 파일이 같은 값(`borderRadius: 20, padding: 22`)을 복사해 들고 있던 것을 여기 하나로 걷었다 —
 *   카드 안 gap(10/14/16)·marginBottom 은 화면 결이라 화면이 쥔다.
 * 🔑 정본 축은 `docs/디자인_토큰.json`(SYNK-appsscript)의 율 — 앱이 그 축을 기계로 소비하는
 *   첫 통로 후보가 이 상수다(값이 정본에서 바뀌는 날 여기 한 곳만 따라간다).
 */
export const 판눈금 = Object.freeze({ 반경: 20, 여백: 22 });

/**
 * 눌림·잠김의 불투명 층 — 이미 «한 벌»로 굳은 두 값만 걷었다(값 변경 0).
 *   버튼 = 주버튼 눌림(pressed) · 잠김 = 비활성 흐림(disabled).
 * ⚠ 이 줄의 값·모양은 tests/감사회귀_R4B1.test.js 가 문자열로 문다 — 아래 `눌림감.면`은
 *   여기서 빌린다(한 값을 두 곳이 알면 갈린다 · 면 값의 정본은 이 줄 하나).
 */
export const 눌림층 = Object.freeze({ 버튼: 0.82, 잠김: 0.35 });

/**
 * 눌림 불투명의 «두 단» — 전 화면 pressed 가 이 둘만 쓴다(감사 D5-13 · 여섯 벌을 둘로 걷었다).
 *   면 = 면 버튼(주버튼·칩·저장·확정·표정칸 등) · 글 = 글자 링크(back·곁길·줄버튼·연한단추).
 * 🚫 pressed 곁에 opacity 숫자 리터럴을 새로 적지 않는다 — tests/감사회귀_W2C4.test.js 가 잰다.
 */
export const 눌림감 = Object.freeze({ 면: 눌림층.버튼, 글: 0.7 });

/**
 * OS 글자 확대는 본문에선 살린다 — «고정 치수 상자 안» 글자만 이 값으로 캡.
 * RN 0.86+React 19 에서 Text.defaultProps 는 소비되지 않아 전역 한 줄이 불가(08-31 실측)
 * — 자리별 maxFontSizeMultiplier prop 이 정본이다. App.js 겉테줄도 같은 상수를 쓴다(G3-6).
 */
export const 글자배율상한 = 1.2;
