'use strict';
/**
 * 테마 — 브랜드 정본의 기계 번역.
 * 원천 = SYNK-appsscript docs/디자인_컨셉_정본_v1.md (키트 v1.6.1) · docs/브랜드_폰트_정본.md
 *
 * R1 2색 원칙: 한 화면 = 바탕 1 + 잉크 1 + 신호 1점(면적 5% 미만).
 *   → 이 앱에서 신호 1점 = **녹음 버튼**이다. 코랄은 다른 어디에도 쓰지 않는다.
 * R2 위계는 밀도로: 크기·색 남발 대신 여백과 알파 층으로.
 * R3 타이포 불가침: 글자에 그림자·왜곡 금지.
 *
 * 알파 하한(정본 v1.4.1 · 유호님 확정): 태그 ≥75% · 서브 ≥72% · 메타 ≥62%.
 */

export const 색 = {
  바탕: '#0F1730', // Navy 2 — 다크 면은 전부 Navy 4단 안에서
  바탕띄움: '#131A32', // Navy Ink — 카드 한 층 위
  잉크: '#F6F1E8', // Cream
  신호: '#FF6B5C', // Coral — 녹음 버튼 전용
  // 알파 층 — 정본 하한을 rgba 로 고정
  잉크_태그: 'rgba(246,241,232,0.75)',
  잉크_서브: 'rgba(246,241,232,0.72)',
  잉크_메타: 'rgba(246,241,232,0.62)',
  잉크_희미: 'rgba(246,241,232,0.28)', // 선·비활성 도형 전용(글자 금지 — 하한 미달)
  /**
   * 보조 잉크 2 (Slate · 유호님 확정 2026-08-07) — **3번째 글자 층** 전용.
   * 「위계는 밀도」는 1·2층의 규칙이고 3층에선 더 내려갈 자리가 없어 위계가 무너진다.
   * 🔴 다크판 Slate 의 허용 바닥은 **Navy 2·Navy·Navy Ink 뿐**이다(Navy 3 위는 4.00 미달).
   *   이 앱의 면은 바탕(Navy 2)·바탕띄움(Navy Ink) 둘뿐이라 둘 다 허용 바닥이다.
   */
  잉크_보조: '#8A93AD',
};

/**
 * 🔑 **R1 「신호 1점」이 화면마다 어디인지 못박는다.**
 *   말하기 화면 = **녹음 버튼**(코랄 면).
 *   인증 화면  = **오류 메시지**(코랄 글자 — 다크 바탕에서만 글자로 쓸 수 있다).
 *   같은 화면에 둘을 함께 두지 않는다 — 인증 화면에는 녹음이 없어서 성립한다.
 * 🚫 인증 화면의 **버튼을 코랄로 칠하지 않는다.** 기본 버튼은 Cream 면 + Navy 2 글자(대비 15.74)다
 *   — 코랄 면 위 글자는 Ink·Navy 2 만 허용이라 「색 버튼 + 흰 글자」라는 세상의 기본값이 여기선 미달이다.
 */
export const 신호자리 = { 말하기: '녹음버튼', 인증: '오류메시지', 답장: '오류태그', 도착확인: '나가기', 교수멘탈: '보내기버튼', 나침반: '적기버튼' };

/** 폰트 패밀리 키 — App.js 의 useFonts 등록명과 일치해야 한다. */
export const 폰트 = {
  본문: 'SUIT-Medium', // 500
  강조: 'SUIT-SemiBold', // 600
  헤드: 'SUIT-ExtraBold', // 800
  캡션: 'SUIT-Regular', // 400
  모노: 'DMMono-Medium', // 라벨·타이머 — 로마자·숫자·기호 전용(키릴·한글 글리프 없음)
};

/** DM Mono 라벨 트래킹 — 정본: 짧은 대문자 라벨일수록 크게 벌린다(+0.02~0.24em). */
export const 모노트래킹 = { 라벨: 2.4, 타이머: 0.6 };

/**
 * 몽골어(키릴) 글자 — **킷 폰트에 이 문자가 없다.**
 *
 * 🔴 `assets/fonts` 에 실린 것은 SUIT 4종 + DM Mono 뿐이고 넷 다 키릴 자형이 없다. 정본이 정한
 *   몽골어 폰트는 **Inter Tight cyrillic-ext** 인데 아직 미탑재고, 선행 조건(`ө`·`ү` 자형 육안
 *   실측)이 P0 §188·`docs/동의_문구_v1.md` 에 열려 있다.
 * 🔑 그래서 `fontFamily` 를 **일부러 비운다** — 기기 시스템 폰트가 키릴을 그린다. SUIT 을 지정하면
 *   글리프가 없어 **두부(□□□)** 가 뜨는데, 그 화면은 「글자가 안 온 것」과 구별이 안 된다.
 *   ⚠ 자리를 여기 한 곳에 두는 이유: 몽골어를 그리는 화면이 앞으로 여럿이다(답장 해설·동의문·
 *   막힘 안내 병기). 화면마다 정하면 한 곳이 SUIT 을 쓰고 그 화면만 두부가 된다.
 * 👉 Inter Tight 가 실리는 날 **이 상수에 `fontFamily` 한 줄만** 더한다.
 *
 * 쓰는 법 — 색은 화면이 정한다: `해설: { ...몽골어, color: 색.잉크_서브 }`
 */
export const 몽골어 = { fontSize: 15, lineHeight: 25 };
