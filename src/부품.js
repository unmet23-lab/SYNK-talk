'use strict';
/**
 * 부품 — Loom 이 구운 펠트 UI 조각을 «화면에 세우는» 한 통로 (08-28).
 *
 * ■ 왜 이 파일이 있나
 *   `assets/부품/` 에 28장이 서 있는데, 화면마다 `require` 와 가로세로비를 따로 적으면
 *   그 수가 곧 여러 곳에 산다. 치수는 **자른 이**(`tools/부품변환.py`)가 `치수.json` 에 적고
 *   화면은 이름만 부른다.
 *
 * ■ 🔑 이름 대응표 — «공방 판» 이름 → talk «화면 파일» (Loom 앱반입 설계 §5 · 09-01)
 *   형제 저장소(SYNK-appsscript)의 요소사전·설계는 «공방 판» 이름으로 자리를 적는데, 그 이름과
 *   talk 의 화면 파일이 **1:1이 아니다.** 사전을 읽고 앱을 열면 매번 여기서 어긋난다.
 *   🔴 **표는 여기 한 곳에만 둔다** — 형제 저장소에도 같은 표를 적으면 두 정본이 갈린다
 *     (같은 것을 두 곳에 두어 갈린 사고 · `CLAUDE.md` 머리말).
 *
 *     「오늘」       = `말하기화면.js` (126KB · 앱 최대 파일)
 *     「퀴즈」       = **단일 화면이 아니다 — 게임 4갈래**이고 `말하기화면.js` 가 고른다:
 *                     G1 `교수멘탈화면.js`(732행) · G2 `보고서교정화면.js`(742행)
 *                     G3 `알바변명화면.js`(749행) · G4 `서류관문화면.js`(763행)
 *     「나의 성장」  = `어제의나.js`
 *     「소식」       = `답장화면.js`
 *     「오늘의확인」 = **전용 화면이 없다.** `교수멘탈화면.js`(386행) 안에 prop 으로 그려지고
 *                     (「새 화면 0 · 이미 보는 자리」 규칙), 판정 정본은 `lib/성향확인.js` 다.
 *
 * ■ 🔴 여기 «쓰는 것만» 적는다 — 28장을 다 적지 않는다
 *   Metro 는 `require` 된 자산만 번들에 싣는다. 안 쓰는 24장을 여기 적으면 학생 앱이
 *   그만큼 무거워진다(장당 100~240KB). **자리가 정해진 것만 한 줄씩 는다.**
 *
 * ■ 🔑 왜 28장 중 이것뿐인가 (08-28 조사 · 유호 「어디에 쓸지 정해서 배선해줘」)
 *   구운 28장은 **UI 사전**이지 앱 부품이 아니었다. 셋으로 갈린다 —
 *   ①**값이 그림에 구워진 것 9**(뱃지 1·3·12 · 스테퍼 2·4·7 · 슬라이더 025·058·085):
 *     그림이 «임의의 수»를 못 보여주니 데이터에 못 붙는다.
 *   ②**앱에 그 조작이 없는 것 9**(모달·토스트·탭전환·스와이프·드래그·풀다운·라디오·
 *     단추위계·고르개): 실측 — `Modal` 0벌 · `toast` 0벌 · 그 제스처 0벌.
 *   ③**화면 규율이 막는 것**: 답장화면은 「🚫 면·배지를 안 만든다 — 배지는 점수의 다른
 *     이름이다(철학 Ⅱ-8)」, 어제의나는 「색을 더하지 않고 밀도로만 위계를 준다(R2)」,
 *     여러 화면이 「신호 1점」(테마 R1)을 진다. **앱이 일부러 비어 있다.**
 *   ⇒ 그래서 «화면이 이미 말하려는 것을 그림이 대신 말해 주는» 자리만 골랐다.
 *     나머지는 자리가 서는 날 한 줄씩 는다 — 억지로 넣는 것은 명품화가 아니라 장식이다.
 *
 * ■ 🔴 1보 반입 판정 — 관문 넷을 28장에 **실제로 걸었다** (09-01 · 설계 §1 · 실측)
 *   설계 §2 의 1보 후보 넷 중 **셋이 talk 에 없다**: 「구분선(러닝 스티치)」·「텍스트 필드
 *   (단춧구멍 슬롯)」·「스냅」은 형제 저장소에서 구워졌지만 `assets/부품/` 로 **변환된 적이 없다.**
 *   변환된 28장은 조작·조작2·표시2·구조2 세트뿐이다. 남은 후보 「접힘」은 실물을 재니 코랄 24.9%.
 *
 *   🔑 **왜 관문 ③이 마지막 관문인가 — 코랄은 이 앱에서 장식이 아니라 «낱말»이다.**
 *     `어제의나.js` 머리말이 그 까닭을 쥔다: 「코랄로 칠하면 그 색이 이 앱에서 뜻하는 것
 *     (**지금 녹음 중**)이 흐려진다」. 유채가 든 조각은 그 화면의 신호를 «둘»로 만드는 데 그치지
 *     않고 **틀린 낱말을 말한다.** 그래서 「면적이 작으니 괜찮다」로 못 넘어간다.
 *
 *   실측 — 코랄 면적(프레임 대비 · 밝기 바닥 V≥0.30 · S≥0.30 · 색상 340~28°):
 *     **내려놓은바늘 0.00% · 녹음_대기 0.03%** ← 무채는 이 둘뿐이고 **둘 다 이미 실렸다**
 *     라디오 0.31 · 시접자 1.07 · 코치마크 1.25 · 녹음_말하는중 1.59 · 녹음_맺음 2.05
 *     스와이프 3.25 · 모달 3.53 · 파이핑 4.78 · 토스트 6.90 · 탭전환 11.6 · 풀다운 12.1
 *     주머니 18.0 · 단추위계 19.4 · 고르개 23.0 · 드래그 23.5 · 접힘 24.9 · 뱃지 37~39 · 읽음표시 43.3
 *
 *   ⇒ **안 실은 24장이 어느 관문에서 걸렸나**(싼 순):
 *     · **파이핑** 87KB — ①②④ 통과. 자리도 있다(`검수화면.js:1025`·`답장화면.js:378` 의 1px
 *       실금이 지금의 구분선이다). **③에서만 걸린다**: 코랄 심 한 줄이 구분선마다 깔리고,
 *       두 화면은 코랄을 이미 한 번 쓴다(`검수화면.js:1010` 단추 · `답장화면.js:367` 태그) → 신호 둘.
 *     · **접힘** 112KB — ①②④ 통과(접이식이 `강사화면.js:265·401·430`·`검수화면.js:566` 에 실재).
 *       ③ 코랄 24.9% 로 기각 — 게다가 한 장이라 편 상태·접은 상태를 못 가른다.
 *     · 라디오·단추위계·고르개·드래그·풀다운·스와이프·탭전환·토스트·모달 — 관문 ② **0벌 실측**
 *       (`Modal` 0 · `RefreshControl` 0 · `Picker` 0 · 탭 네비 0 · 제스처는 `마스코트.js` 쓰다듬뿐).
 *     · 뱃지 3 · 스테퍼 3 · 슬라이더 3 · 읽음표시 — 관문 ④(값·상태가 그림에 구워졌다).
 *     · 시접자·코치마크 — 앱에 에디터 정렬도 튜토리얼도 없다(②).
 *
 *   ⇒ 🔴 **변환된 28장 중 관문 넷을 다 지나는 것은 0장이다**(이미 실린 넷을 빼면). 통로를
 *     「가장 싼 것」으로 증명하려면 **무채 구분선(러닝 스티치)** 한 장이 필요한데 그 장이
 *     talk 에 없다 — 굽기·변환은 형제 저장소 트랙이라 유호님 판정 자리다.
 */
import { Image, StyleSheet, View } from 'react-native';

const 치수 = require('../assets/부품/치수.json');

/* 이름 → 자산. **자리가 정해진 것만** 있다(위 🔴). */
const 그림 = {
  내려놓은바늘: require('../assets/부품/구조2/내려놓은바늘.webp'),
  /* 🔑 1보 ②가 여기서 닫혔다(09-01) — 관문 넷을 다 지난 **첫 새 부품**이다.
     코랄 면적 **0.000%**(내려놓은바늘과 동률 · 파이핑 4.78 은 그래서 기각됐다) ·
     값이 그림에 없다 · 자리는 `검수화면.js` 폐기판(그 화면의 1px 실금을 대신한다).
     7.7KB — 변환분 28장 중 가장 싸다(다음이 스테퍼 13KB · 파이핑 87KB). */
  구분선: require('../assets/부품/구분선/구분선.webp'),
};

/**
 * @param {keyof 그림} 이름
 * @param {number} 폭   가로 px. 세로는 «자른 이»가 적은 치수가 정한다.
 */
export default function 부품({ 이름, 폭 = 220, 스타일, 설명 = null }) {
  const 그것 = 그림[이름];
  if (!그것) throw new Error(`🔴 부품 «${이름}» 이 없다 — src/부품.js 에 한 줄 늘려야 한다`);
  const 잰것 = 치수[`구조2/${이름}`] || 치수[Object.keys(치수).find((k) => k.endsWith(`/${이름}`))];
  if (!잰것) throw new Error(`🔴 치수장부에 «${이름}» 이 없다 — python tools/부품변환.py 를 돌려라`);
  return (
    <View
      style={[{ width: 폭, height: Math.round((폭 * 잰것[1]) / 잰것[0]) }, 스타일]}
      accessible={!!설명}
      accessibilityRole={설명 ? 'image' : 'none'}
      accessibilityLabel={설명 || undefined}
    >
      <Image source={그것} style={s.그림} resizeMode="contain" accessibilityIgnoresInvertColors />
    </View>
  );
}

const s = StyleSheet.create({
  그림: { width: '100%', height: '100%' },
});
