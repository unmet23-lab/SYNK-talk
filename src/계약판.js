'use strict';
/**
 * 앱이 선언하는 계약판 (C0 §6) — **한 글자짜리 파일이 따로 있는 이유가 있다.**
 *
 * 🔑 **계약 JSON 을 import 하지 않는다.** 그 파일은 28KB 짜리 개정 이력이고 앱이 쓰는 것은
 *   이 두 글자뿐인데, import 하면 그 28KB 가 통째로 번들에 인라인돼 몽골 모바일 회선으로 나간다.
 *   대신 손 상수를 두고 **`tests/계약.test.js` 가 정본과 같은지 기계로 묶는다** — 갈라지면
 *   CI 가 빨개진다. 갈라진 채 나가면 증상은 426 이고 학생 눈에는 「업데이트하래요」로 보인다.
 *
 * ■ 왜 `src/과제API.js` 에서 떼어냈나 (2026-08-09 · C0 §7 왕복 6-⑥)
 *   상수가 거기 살면 **통로(`src/사건통로.js`)가 과제 API 를 import 해야 하고**, 그래서 과제
 *   조회는 통로를 거꾸로 못 쓴다(순환 import). 그 한 줄 때문에 조회 셋이 각자 fetch 사본을
 *   들고 있었고, 만료 갱신을 통로에 세운 날 **과제 조회만 갱신 없이 남는** 자리가 됐다.
 *   문을 하나로 모으려면 상수가 어느 문에도 안 살아야 한다 — 그래서 파일이 이것뿐이다.
 */
export const 계약판 = 'c10';
