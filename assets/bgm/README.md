# BGM — 미니게임 측정 스테이지 (생성물 폴더)

> **WAV 는 git 에 없다**(1MB 가드 · 생성물) — **정본은 `tools/BGM빌드.js`**(결정적 렌더 · 같은 코드 = 같은 바이트).
> `npm install`(postinstall)이 재생성하고, `node tools/BGM빌드.js --check` 가 손 편집을 잡는다.

- 유호 확정 08-11: **기본안**(공통 1곡 · 측정 템포 고정 `측정템포BPM` 1곳 · C 펜타토닉 · 녹음 구간 자동 음소거) + 효과음·햅틱 병행.
- **유호 피드백 08-11(a·b 시청)**: 「깔리는 소리 너무 좋아 · 게임소리같이 튀는 소리는 빼자」 → 종·아르페지오 층 전면 제거(a·b 은퇴 — 도구가 재생성 시 정리). **튀는 층 복원 금지** — 멜로디는 효과음 3종의 몫.
- ⏳**선정 대기**: 후보 `synk-bgm-candidate-c.wav`(깔림 — 패드+맥동만) — 유호님 시청 확정 후 `synk-bgm-measure.wav` 로 남긴다(§3-2 ⓑ 순서 밖 금지 · 회귀 `tests/소리.test.js` 가 폴더 상태를 문다).
- 재생은 `src/소리.js` 만 — 전역 게이트(녹음 중 음소거)가 그 한 곳에 산다.
